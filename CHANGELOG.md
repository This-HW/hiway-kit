# Changelog

All notable changes to claude-code-kit are documented here.

Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

---

## [Unreleased]

## [3.34.1] — 2026-09-11

### Fixed — Codex 가 훅 주입을 **가운데에서** 잘라 규범이 사라지고 있었다

Codex 는 SessionStart 훅의 `additionalContext` 가 **2,500 토큰**을 넘으면 전문을
`<tmp>/hook_outputs/<thread>/<uuid>.txt` 에 쓰고 모델에게는 **머리·꼬리 미리보기**만 준다.
토큰은 `ceil(bytes / 4)` 로 센다. 이 킷의 출력은 그보다 크다. rollout 원문을 기대 출력과
대조한 결과(모델 자기보고 아님):

| 세션 | 훅 출력 | 모델에 도착 | 사라진 규범 |
| --- | --- | --- | --- |
| 이 킷 워크트리 (원장 있음) | 16,622B | 10,028B | Feedback Loop · Loop Engineering · Parallel Worktree |
| 다른 레포 워크트리 (원장 없음) | 13,843B | 10,026B | Parallel Worktree |

출력 순서가 WORKFLOW → active work → LESSONS → stale → RULES 라 **가운데인 RULES 가**
잘린다. 머리와 꼬리(`=== END RULES ===`)는 살아 있어 겉보기엔 온전하다.

**상류 출처**: `openai/codex` 의 `codex-rs/hooks/src/output_spill.rs`
(`DEFAULT_HOOK_OUTPUT_TOKEN_LIMIT = 2_500`) · `codex-rs/utils/string/src/truncate.rs`
(`APPROX_BYTES_PER_TOKEN = 4`) · `codex-rs/config/src/hook_config.rs` 의 핸들러 필드
`additionalContextLimit` (PR #34393 에서 도입) — *"Unset uses 2,500 tokens; `0` disables
spilling for this hook."* 필드를 모르는 옛 Codex 는 이 필드를 **무시**하고 지금처럼
자른다. 깨지지는 않는다.

**고친 것**: 생성되는 `hooks-codex.json` 의 SessionStart 엔트리에
`"additionalContextLimit": 0` 을 넣었다(`packaging/targets.json` → `build-targets.py` 가
그대로 전달, 0 이상의 정수가 아니면 생성 단계에서 거부).

**왜 유한값이 아니라 0 인가.** 유한값을 고르면 `RULES_PEAK_CAP` 과 **따로 움직이는 두 번째
숫자**가 생긴다. 이번 결함이 정확히 그것이었다 — Codex 의 숫자와 우리 예산을 아무도
대조하지 않았다. 0 이면 한도의 소유자는 우리 예산 게이트 하나다: 규범은
`check_injection_budget.py`, LESSONS 는 `feedback_ledger.DIGEST_CHAR_CAP`, active work 와
stale 은 개수 상한이 묶는다.

**게이트 — 이 결함을 잡았을 축을 넣었다.** `check_injection_budget.py` 에 넷째 축
**호스트 전달 한도**를 추가했다. `packaging/targets.json` 의 session-start 훅마다 한도를
구해(키가 없으면 상류 기본값 2,500), 0 이면 통과, 유한값이면
`ceil((RULES_PEAK_CAP + LESSONS 최악) / 4) ≤ 한도` 여야 통과한다. 필드를 지우면 red 가 된다.

> **⚠️ 사용자 조치 — Codex 에서 훅을 한 번 다시 신뢰해야 한다.** Codex 는 핸들러 설정
> **전체**를 해시해 신뢰 여부를 기억하므로(`discovery.rs::hook_hash`), 필드가 추가된 이
> 버전은 새 훅으로 취급된다. 업데이트 후 대화형 세션에서 신뢰 프롬프트를 승인하라.
> 승인 전에는 비대화형(`codex exec`·CI)에서 훅이 **경고 없이 건너뛰어진다**.

Claude Code 는 SessionStart 주입을 자르지 않는다(2.1.268 트랜스크립트에서 `=== END RULES ===`
까지 온전). 그래서 Claude 쪽 축은 만들지 않았다.

### ★정정 — 그동안 "Codex 도 메모리가 일치한다"고 보고한 것은 틀렸다

3.30.0 의 *"하나의 원장, N 개의 주입기가 두 하네스에서 성립한다"* 는 Codex 가 **LESSONS 첫
항목**을 정확히 인용한 것을 근거로 삼았다. 그런데 머리와 꼬리를 남기는 중간 절단에서는
**첫 줄이 항상 살아남는다.** 그 프로브는 온전 도착과 절단을 **가르지 못했다** — 머리만
확인한 것이다(`warning-signal.md` §측정 9 규칙 3: 두 가설을 가르지 못하는 값은 증거가
아니라 배경이다). 관측(첫 줄 일치)은 참이었고, 그 위에 얹은 추론(*"주입이 온전하다"*)은
거짓이었다.

`packaging/targets.json` 의 `memoryInjection` 라벨을 `[관측: LESSONS 머리 도착 / 추론
'메모리 동일' 은 반증됨]` 으로 고치고 다시 잡는 방법을 적었다. rollout 의 developer 메시지를
기대 출력과 **바이트로** 대조하는 것이다. 이 결함을 가린 또 하나의 기록
`knownLimitation_outputTruncation`(*"상한값 미특정"*)도 상한값·출처·해결책으로 갱신했다.
README 의 Codex 전달 표에도 같은 사실을 적었다.

## [3.34.0] — 2026-09-10

### Changed — 강등 사다리를 3단으로: **Codex 도 격리 위임이 된다**

v3.32.0 의 강등 경로는 *"위임 수단이 없으면 세션에서 직접 수행 — 격리 이점은 사라진다"*
였다. **그 전제가 Codex 에 대해 틀렸다.** 실측: Codex 세션은
`collaboration.spawn_agent` · `wait_agent` · `list_agents` · `send_message` ·
`interrupt_agent` · `followup_task` 를 호출할 수 있고, **실제로 왕복한다.**

앞서 이 항목을 *"구조적으로 불가"* 로 적었던 근거는 **모델 자기보고 하나**였다 —
*"hiway-kit 플러그인의 서브에이전트가 있나"* 에 `NO` 를 받고 *"Codex 에는 서브에이전트
개념이 없다"* 로 **일반화**했다. 앞 답은 정확했다(킷 에이전트는 등록되지 않는다).
일반화가 틀렸다. `[관측 n=1 / 추론 미검증]` 을 `[confirmed]` 처럼 쓴 것이다(§측정 9).

| 단 | 수단 | 격리 |
| --- | --- | --- |
| 1 | 네이티브 서브에이전트 | 있음 |
| **2 (신설)** | **호스트가 제공하는 위임 수단** — 에이전트 정의 본문을 **요약 없이** 지시문으로 넘긴다 | **있음** |
| 3 | 세션 내 직접 수행 | 없음 (강등 표시 유지) |

**2단이 "사라진다"고 적었던 격리를 되살린다.** 적대적 리뷰에서 격리는 값의 일부이므로
문구 개선이 아니라 **품질 복원**이다. 스킬 본문에는 **도구 이름을 0건** 두었고(배포물이
특정 하네스를 전제하면 다른 하네스에서 그 지시가 죽는다), 구체 수단·레시피·함정은
`docs/control-loop-transport.md` 부록에만 적었다.

**종단 확인**: `review-code` 정의 본문 **26,336B** 를 그대로 넘겨 계약 형식 리포트
(`## 판정: [REJECT] … ## 완료:`)를 회수했다. 플러그인의 `agents/` 는 설치본에 복사되지만
서브에이전트로 **등록되지는 않으므로**(실측), 다리는 킷이 놓는다.

### ★위조 회신 — 하네스의 "했다"를 근거로 쓰지 마라

2단을 검증하던 첫 회차에서 **Codex 가 아무것도 띄우지 않은 채 프롬프트의 토큰을
"받았다"며 회신했다.** 이벤트 스트림을 보고서야 적발됐다. 오늘 같은 형태를 세 번째로
만났다(컨텍스트 내용 오보고 · `mcp__` 오보고 · 이번 위조).

그래서 2단 문구에 **"회신은 자기보고다"** 를 넣고, 부록에 함정으로 적었다.
**위임이 실제로 일어났는지는 산출물과 이벤트 기록으로 확인한다.**

### 실환경 검수 — 사용자 Codex 세션과의 메모리 일치 (2026-09-10)

사용자의 실제 Codex 세션을 상대로 검수했다. **결론: 메모리는 일치하고, 막는 것은 훅 신뢰뿐이다.**

- **학습 원장 digest**: 그 세션이 보고한 문장이 컨트롤 로컬 출력과 **바이트 일치**.
- **공유 규범 9종**(code-quality·definition-of-done·feedback-loop·loop-engineering·
  parallel-worktree·planning-check·planning-protocol·ssot·untrusted-text):
  섹션별 sha256 **전부 일치**.
- **차이는 의도된 2종뿐** — `mcp-usage`·`task-resume` 는 Claude Code 고유 프리미티브를
  말하므로 v3.33.0 이 Codex 에서 제외한다. 그것을 가리키던 색인 줄도 함께 빠진다.
  **두 하네스가 같은 규범 위에서 일한다** — 빠진 것은 그 하네스에 해당되지 않는 것뿐이다.
- **막는 것**: 플러그인 훅이 **신뢰되기 전까지 경고 없이 건너뛰어진다.** 같은 설치본에서
  신뢰 없으면 훅 6줄·`NO-LESSONS`, `--dangerously-bypass-hook-trust` 를 더하면 훅 8줄·
  LESSONS 도착. **차이는 신뢰뿐이다.** 비대화형 부여 경로는 없다(README 에 명시돼 있다).


## [3.33.0] — 2026-09-10

### Fixed — 훅 주입이 진입점 파일과 **다른 기준**으로 걸러 Codex 에 Claude 전용 규범을 보냈다

두 전달 경로가 서로 다른 기준을 쓰고 있었다:

| 경로 | 거르는 기준 |
| --- | --- |
| 진입점 파일 (`export_harness.py`) | **`portable`** |
| 훅 주입 (`session-start.py::load_rules`) | **`tier` 뿐** |

v3.30.0 에서 Codex 도 훅으로 규범을 받게 되면서 그 비대칭이 **곧바로 결함**이 됐다.
실측: `mcp-usage`(portable:false) **본문이 통째로** 주입되고(`mcp__` 툴 이름·에이전트
`tools:` allowlist 를 말한다 — Claude Code 플러그인 개념), `task-resume` 도 동일하며,
`reference` 티어 색인 줄이 **없는 수단을 가리켰다**:

```
- 서브에이전트 위임 전 rules/agent-delegation-chain.md 를 읽어라
- 에이전트 선택·모델 정책은 rules/agent-system.md 를 읽어라
```

둘 다 `portable:false` 이고 `Task`·`subagent` 를 전제하는데 **Codex 에는 그 수단이 없다.**
스킬 4종(v3.32.0)과 같은 결함 클래스인데 **이쪽은 자동이고 매 세션**이라 더 나쁘다.

`load_rules` 에 이식성 필터를 **옵션(기본 OFF)** 으로 넣고 **본문과 색인 줄 양쪽**에
적용했다. Codex 훅 매니페스트만 `--portable-only` 를 싣는다 — **런타임 추측으로 하네스를
판별하지 않는다**(그건 조용히 틀린다).

**규범을 재분류하지 않았다.** `mcp-usage` 가 실제로 이식 가능한지(Codex 에도 MCP 가 있다)는
별개 판단이고 이 배치 밖이다. 고친 것은 **두 경로가 같은 선언을 다르게 취급하던 것**뿐이다.

### 검증 — Claude Code 회귀 0, Codex 실물 확인

- **기본 경로 불변**: 필터 OFF 에서 19,398B, `mcp__`·`agent-system` 색인 그대로.
  기본값을 True 로 뒤집으면 **기존 Claude Code 테스트 9건이 먼저 깨진다**(안전망 실측).
- **필터 경로**: 13,672B(−5,726B), `mcp__` 없음, 두 색인 줄 없음.
- **실물 A/B**: 격리 `CODEX_HOME` 에 실제 설치하고 `codex exec` 를 돌려 **rollout 원문**에서
  확인 — `mcp__` False · `agent-system` False · `agent-delegation-chain` False ·
  `=== RULES ===` True · `LESSONS` True.

> **모델 자기보고를 근거로 쓰지 않았다.** 같은 세션에서 모델에게 *"컨텍스트에 `mcp__` 가
> 있나"* 라고 묻자 **`YES` 라고 틀리게 답했다.** rollout 원문은 `False` 다. 하네스가
> 무엇을 받았는지는 **모델이 아니라 기록**이 말한다.

### 주입 예산 — 축을 늘리지 않는다

필터가 켜지면 Codex 쪽 주입량이 **줄어든다**(−5,726B). 상한은 최악의 경우를 재는
것이므로 Claude Code 기준이 곧 상한이고, Codex 전용 축은 지금 필요 없다.
필요해지는 시점은 **Codex 쪽이 더 커질 때**이고 그때 다시 본다.


## [3.32.0] — 2026-09-10

### Fixed — 스킬 4종이 Codex 에 **없는 도구를 쓰라고 지시**하고 있었다

Codex 세션은 이 킷의 스킬 **21종을 전부 인식**한다(실측). 그런데 `debug`·`review`·`test`·
`skill-creator` 넷은 본문에서 `Task tool 사용:` + `subagent_type:` 으로 서브에이전트
위임을 지시하는데 **Codex 에는 그 수단이 없다**(같은 실측에서 확인).

**"수단이 없어 그냥 진행"보다 나쁘다** — 없는 도구를 쓰라고 지시하면 모델이 실패하거나,
지시를 무시하고 임의로 진행하거나, **하지 않은 위임을 했다고 보고**한다. 오늘
`rules/parallel-worktree.md` 를 운송 중립화한 것과 **같은 결함 클래스**이고, 그때 판정은
**"규범 없음보다 나쁘다"** 였다.

**계약과 운송을 분리했다.** 불변식은 각 위임 블록의 `prompt:` 에 적힌 계약이고
(페르소나·검사 항목·출력 형식·완료 선언), 위임은 그 계약을 실행하는 **한 가지 수단**이다.
수단이 없는 하네스에서는 **같은 계약을 세션에서 직접 수행**한다.
*"수단이 없다고 단계를 건너뛰지 마라. 하지 않은 위임을 했다고 보고하지도 마라."*

**대가를 숨기지 않았다.** 직접 수행은 컨텍스트를 공유하므로 **격리의 사각지대 분리 효과가
없다** — 특히 적대적 리뷰에서 격리는 값의 일부다(코드를 쓴 세션이 리뷰하면 작성 시의
가정을 그대로 물려받는다). 그래서 직접 수행한 리뷰는 **강등된 리뷰**로 표시하게 했다:
`## Scope Used: … (직접 수행 — 격리 없음)`.

`skill-creator` 는 **생성 템플릿에도** 강등 경로를 넣었다 — 그러지 않으면 새로 만들어지는
스킬마다 같은 결함이 복제된다.

### Added — §23 게이트: 위임을 지시하는 스킬은 강등 경로를 가져야 한다

4종을 고친 것은 **인스턴스 수정**이다. 다음 달 새 스킬이 `subagent_type:` 을 쓰면
**조용히 재발**한다 — 이 레포가 반복해서 배운 형태다. 그래서 검사로 고정했다.

대상은 나열하지 않고 `skills/*/SKILL.md` 에서 **파생**한다(새 스킬은 놓이는 것만으로
대상이 된다). 되돌려-FAIL 3종 실측: 강등 문구를 지우면 red · **강등 없는 새 스킬을
추가하면 red** · 파생 결과 0건도 red. 게이트 총계 29 → **30**.

### Added — `setup.sh` 가 Codex 도 설치한다 (묻고 나서, 없으면 침묵)

`codex` 가 PATH 에 있을 때만, **묻고 나서** 설치한다 — 사용자 소유 `~/.codex/**` 를
스크립트가 조용히 고치지 않는다. 비대화형(파이프·CI)에서는 묻지 않고 건너뛴다.
`codex` 가 없으면 **아무것도 인쇄하지 않는다**(상시 참인 안내는 소음이다).
설치 후 **훅 신뢰 단계를 안내**한다 — 신뢰 전까지 훅은 조용히 건너뛰어지기 때문이다.

### Fixed — README 파리티 문장이 스스로 모순돼 있었다

*"에이전트와 훅은 Claude Code 심화 기능"* 이라 해 놓고 바로 다음 문장에서
*"비-Claude 하네스가 잃는 것은 정확히 하나 — 자동 차단(훅)"* 이라고 적었다. 둘은
양립하지 않고, v3.30.0(Codex 훅 배송) 이후로는 **앞 문장도 낡았다.**

측정된 표로 교체했다 — 규범·원장·스킬·서브에이전트·자동차단 다섯 축을 하네스별로.
결론 문장도 정정: **비-Claude 하네스가 잃는 것은 전용 실행자(서브에이전트)와 자동
차단이다. Codex 에 대해서는 주입이 더 이상 그 목록에 없다.**


## [3.31.0] — 2026-09-10

### Fixed — 위임 계약이 보고의 **형식**만 정하고 **전달 수단**을 안 정했다

`rules/delegation-contract.md` 의 완료 보고 절은 정규식으로 형식을 고정하면서 *어디로*
보낼지는 말하지 않았다. 그래서 워커가 **자기 터미널에 출력하고 끝낼 수 있다** — 일은
끝났는데 컨트롤에는 아무것도 안 온다.

실측 `[confirmed: n=2, 서로 다른 프로젝트, 같은 날]`. 한쪽에서는 조사 세션 둘이 27분간
무응답이라 '죽었나' 로 읽었는데 조회해 보니 **정상 작동 중**이었고 결과를 자기 터미널에
쓰고 있었다. 다른 쪽(이 레포)에서는 워커가 **커밋 2개와 clean 트리를 남기고 완료 보고만
보내지 않아**, 컨트롤이 60분 침묵을 고장으로 읽고 개입해 직접 검증했다.
**두 경우 모두 일은 끝나 있었다** — 잃은 것은 산출물이 아니라 시간과 판단이다.

수단은 호스트마다 다르므로 계약이 지정하지 않는다. 지정하는 것은 **명시 의무**다:
*"보고는 `<수단>` 으로 `<컨트롤 식별자>` 에 보낸다. 자기 터미널에 출력하는 것은
컨트롤에 닿지 않는다."*

그리고 **부분 보고를 허용이 아니라 요구로** 바꿨다 — 막혔으면 막힌 지점만 보내고,
부분이면 부분이라고 선언한다(§측정 6). **침묵이 실패다.** 컨트롤은 침묵을 성공으로도
실패로도 읽을 수 없다.

### Fixed — 병합 **전** 게이트만으로는 부족하다 (`control-loop`)

스킬은 *"컨트롤이 병합 전에 게이트를 직접 실행한다"* 고만 규정했다. 그 실행이 **어디서**
도는지는 말하지 않았고, 격리된 곳에서 도는 검증에는 두 빈틈이 있다:

1. **격리 환경에는 런타임 산출물이 없다.** 그 부재가 안전한 기본값으로 흡수되면 그
   단언은 **공허하게 참**이 된다 — 한 번도 평가되지 않은 채 초록이다.
2. **워커는 다른 워커의 변경을 보지 못한다.** A 가 만든 코드를 B 가 강화한 검사가 잡는
   상황은 양쪽 워크트리 **어디에서도** 드러나지 않는다.

실측 `[confirmed: n=2]`. 한쪽은 병합 게이트 10회가 전부 초록이었는데 통합 브랜치에서
전량을 돌리자 **안전 게이트 3건이 red** 였다(1번). 이 레포는 워커 3인이 각자 초록이었는데
병합 후 **게이트 2건이 red** 였다(2번 — 어느 워커도 혼자서는 볼 수 없는 결함).

**기전은 다르지만 처방은 같다**: 통합 브랜치에서 최소 1회 전량을 돌린다. red 면 병합을
되돌리는 게 아니라 **그 자리에서 고친다** — 통합 지점이 그 결함이 처음 보이는 유일한 자리다.


## [3.30.0] — 2026-09-10

### Added — Codex 가 규범과 **학습 원장**을 훅으로 받는다 (파일이 아니라)

**측정이 판정을 뒤집었다.** 이 킷은 지금까지 *"Codex 훅은 싣지 않는다 — 발화 근거가
없다"* 로 막아 두고 규범을 `AGENTS.md` **파일**로만 보냈다. 그래서 Codex 는 32 KiB 병합
한도에 눌렸고 **LESSONS(학습 원장)는 아예 못 받았다.**

이번에 격리 `CODEX_HOME` 에서 넷을 실측했다:

| 측정 | 결과 |
| --- | --- |
| SessionStart 훅 stdout → 모델 컨텍스트 | **도달한다** (토큰 프로브) |
| 훅 페이로드 스키마 | **Claude Code 와 동일** (`hook_event_name`·`tool_name`·`tool_input`·`cwd`·`transcript_path`) |
| 플러그인이 훅을 배송하는가 | **한다** — `.codex-plugin/plugin.json` 의 `hooks` 필드. 소비자가 `~/.codex/hooks.json` 을 손댈 필요 없다 |
| 플러그인 루트 변수 | **`${CLAUDE_PLUGIN_ROOT}` 가 Codex 에서도 치환된다.** `${CODEX_PLUGIN_ROOT}` 는 **없다**(음성 대조로 확인) |

페이로드가 동일하므로 **훅 스크립트를 새로 쓰지 않았다** — 같은 `session-start.py`·
`auto-format.py` 를 그대로 쓰고, 하네스 차이는 **경로 추출 한 함수**로 흡수했다.
Codex 는 파일을 `tool_name:"apply_patch"` + 패치 봉투로 쓰므로(Claude Code 의
`Edit`/`Write` + `file_path` 와 다르다) 그 한 겹만 더했다.

**싣지 않은 것**: `protect-sensitive`(PreToolUse 차단). `codex exec` 에서 훅이 실행돼도
**명령이 그대로 실행됐다** — 차단이 성립하지 않는다. **돌지 않는 보호를 실으면 소비자가
보호받는다고 믿는다.** 집행은 엔진 무관인 git 훅 층이 담당한다.

### ★한계 — 신뢰(trust) 없이는 **조용히 건너뛴다**

컨트롤이 독립 재현한 결과다. 정책대로 만든 매니페스트를 실제로 설치하고 `codex exec` 를
돌렸더니 **훅이 한 줄도 발화하지 않았고 경고도 없었다** — 모델은 규범을 못 받은 채
정상 응답했다(완료 게이트 명령을 묻자 `NONE`). 같은 설치본에
`--dangerously-bypass-hook-trust` 만 더하자 즉시 `hook: SessionStart` 가 뜨고
**주입된 규범에만 있는 값**(`scripts/verify-done.sh`)을 회신했다. 차이는 신뢰뿐이다.

승인 관문이 있는 것은 정상이고 바람직하다. **문제는 침묵이다** — 비대화형(CI·자동화)에서
신뢰가 없으면 훅이 경고 없이 빠지고, 소비자는 규범이 주입된 줄 안다. 이 킷이 최악으로
다루는 형태와 같다. 그래서 **한계로 명기했고**, 워커 기록에 이 단계가 빠져 있던 것도
같이 적었다 — 스크래치 픽스처와 실물의 차이였다(`warning-signal.md` §측정 1).

### 확인 — **하나의 원장, N 개의 주입기**가 두 하네스에서 성립한다

신뢰를 준 상태에서 kit 레포를 cwd 로 물었더니 Codex 가 **로컬 `feedback_ledger.py digest`
첫 줄과 동일한 문장**을 인용했다. 원장은 `<git-common-dir>/kit/` 에 있어 하네스와 무관하게
한 벌이므로, 이것으로 규범뿐 아니라 **메모리도 Claude Code + Codex 두 하네스에 같은
출처에서 도달**함이 실증됐다.


## [3.29.0] — 2026-09-10

### Fixed — 원장 쓰기의 내구성을 export 쪽 계약과 맞춘다 (리뷰 잔여 마지막 1건)

같은 킷 안에 원자적 쓰기 구현이 **둘**인데 내구성이 달랐다. `_write_ledger` 는
`.tmp.<pid>`(예측 가능) + `os.replace` 로 끝났고 **fsync 도 모드 보존도 없었다.**
`os.replace` 는 **프로세스 사망**에는 원자적이지만 **호스트 크래시**에는 아니다 —
rename 만 반영되고 데이터가 안 반영되면 최대 `CAP` 개의 학습 항목이 **빈/잘린 원장**으로
남는다. 오늘 이 파일에서 유실 결함을 둘 고쳤는데(`promote()` 전체 비우기 ·
`parse_ledger` 의 부재/실패 뭉갬) **세 번째 유실 경로를 열어 둔 채 닫을 수는 없었다.**

`export_harness.py::_atomic_write` 와 같은 계약으로 올렸다 — `mkstemp`(예측 불가,
O_EXCL 원자 생성) · 파일 `fsync` · 디렉토리 `fsync`(실패 무시 — 일부 파일시스템이
지원하지 않고, 여기서 죽으면 fail-open 원칙이 깨진다) · **기존 모드 보존**.

**공용 헬퍼로 뽑지 않았다.** 리뷰는 그렇게 권했으나 두 파일 모두 **킷 내부 의존이 없는
standalone** 이고 플러그인 캐시에서 CLI 로 직접 실행된다 — 공용 import 경로는 그 호출
형태에서 성립하지 않는다. **소비자 환경을 깨는 쪽이 중복보다 나쁘다.** 대신 이 레포의
기존 관례를 따랐다(`_resolve_in_repo` 가 두 파일에 각각 한 벌씩 있고 주석이 계약을
묶는다 — D-15: **구현은 여러 벌, 계약만 하나**). 독스트링에 *"한쪽을 고치면 다른 쪽도
고쳐라"* 를 명시했다.

**심링크 공유 원장(F7)은 그대로다.** 컨트롤이 병합 후 독립 확인했다 — 심링크 유지 `True`,
내용이 링크 대상에 도달, **모드 0o600 보존**(일반 파일 0o640 도 보존), tmp 잔여 없음.
남의 소유 링크를 거부하는 `_symlinked_target_is_safe` 검사도 그대로다.

**부수 — 기존 테스트 하나가 false-green 이 될 뻔했다.** `test_write_ledger_leaves_no_tmp_files`
가 `*.tmp.*` **glob 패턴**으로 잔여를 찾고 있었다. `mkstemp` 로 바꾸면 그 패턴에 안 걸려
**tmp 가 남아도 통과**했을 것이다. 이름 패턴 대신 **"원장 파일 하나만 남았다"** 로 고쳤다 —
검사의 대상을 *이름의 형태*에서 *실제로 원하는 상태*로 옮긴 것이다.


## [3.28.0] — 2026-09-10

### Fixed — 리뷰 잔여 Low 9건 (이로써 이번 전수 리뷰 항목이 전부 닫혔다)

- **frontmatter 파싱이 본문의 `---` 를 frontmatter 로 오인했다.** `split("---", 2)` 는
  파일 어디에 있는 `---` 든 자른다 — frontmatter 가 없고 본문에 수평선이 둘 있는 규범
  파일이면 **본문을 frontmatter 로 파싱**한다. 규범 15종이 전부 frontmatter 를 갖고 있어
  발현하지 않았을 뿐인 **잠복 오파싱**이었다. 이 레포의 기존 관례
  (`check_injection_budget.py::FRONTMATTER_RE` — 줄 앵커 + offset 0)를 그대로 따랐다.
- **`tier` 미선언이 조용히 통과했다.** `portable` 미선언은 red 인데 `tier` 미선언은 `""`
  로 인라인 대상에 들어갔고, `load_rules` 는 그것을 **주입하지 않는다** — 즉 **Claude Code
  는 안 읽는 규범이 다른 하네스에는 나갔다.** 이제 대칭으로 red 다.
- **`rules/VERSION` 읽기 실패의 `"unknown"` 폴백을 제거했다.** 이 값은 sha 입력이라
  일시적 실패 한 번이 진입점 전량 재기록을 **두 번** 유발했다(소비자 레포에 무의미한 diff).
- **`SkipTally.report` 의 인자를 뒤집었다** — 치명 사유 화이트리스트에서 **비치명 예외
  목록**으로. 전자는 새 건너뜀 사유가 목록에 없어 **기본이 노랑**이라 조용히 커버리지를
  잃는다(그 모듈 독스트링이 비판하던 바로 그 구조). 이제 **기본이 red** 이고 노랑으로
  내리려면 호출부가 등재해 정당화해야 한다. 호출부 셋의 예외 목록은 각각 `∅`·`∅`·
  `{비-UTF-8}` 로, 뒤집기가 실질적으로 강해졌음을 확인했다.
- 그 외: category 조용한 재분류에 stderr 한 줄 · `promote()` 의 빈 원장을 `mode:"noop"` 로
  분리 · `verify-done.sh §11` 이 종료코드 3 을 일반 실패로 뭉개지 않음 ·
  `check_installed_hooks` 가 hooks 경로를 저장소당 한 번만 조회.

**L-8 은 인스턴스가 아니라 클래스를 닫았다.** `pre-push` 의 하드코딩된 제품명을 파생
대상에 등재하다가 `reference-transaction`·`pre-commit` 에도 같은 것이 있음을 발견해 셋 다
등재했다(파일 내용은 미변경). §18 파생 대상이 16 → **19** 로 늘었다.

**부수 발견**: 인자를 뒤집자 `test_check_injection_budget.py` 가 **옛 치명 집합을 인라인
리터럴로 넘기고 있던 것**이 드러났다 — 테스트가 상수를 참조하지 않아 정책이 바뀌어도
따라오지 않는 상태였다. 호출부를 고치고 내용 기반 재발 방지 검사를 추가했다.

### Changed — agy 원인 판정 **철회**: 쿼터 가설은 살아 있다

v3.26.0~v3.27.0 에 *"`total_tokens: 0` 이므로 쿼터가 아니다"* 라고 적었다. **그 추론은
거꾸로다** — 쿼터로 **거부되면** 요청이 처리되지 않으므로 토큰이 0 이다. `total_tokens=0`
은 쿼터 가설을 반증하지 않고 **거부된 요청의 모습 그 자체**다. 중립~지지 증거를 반증
증거로 읽었다.

그리고 반증할 실측이 상대 세션 원장에 **이미 있었다**: 8/26~9/08 사이 같은 CLI 의 실패가
`"Individual quota reached… Resets in 69h49m55s"` 라는 **명시적 쿼터 메시지**로 여러 차례
기록돼 있었다. 마지막 것이 `2026-09-08T00:22:13Z` + 69h49m55s → **리셋 `2026-09-10T22:12Z`**.
이 도구는 **지금 자기가 선언한 쿼터 창 안에 있다.**

**내 실수의 형태**: *"쿼터가 아니다"* 는 음성 결과인데 **양성 대조를 붙이지 않았다** —
쿼터일 때 무엇이 보이는지(= 그 원장의 문구)를 대조하지 않았다. 게다가 신호 **하나**로
가설을 죽이면서 *"이 값이 두 가설을 가르는가"* 를 묻지 않았다. 하루 종일 남에게 적용하던
`warning-signal.md` §측정 3 을 정작 내 결론에는 안 걸었다.

**유효한 채로 남는 것**(실측이라 철회와 무관): 완주 0/5 로 **재시도 이득 0** ·
`rc=0` 이라 `agy … && 다음` 은 게이트가 아니며 판정은 `status`/`total_tokens` 로 ·
wall ≫ duration 이라 **실패 1건의 벽시계 비용 = 설정한 `--print-timeout` 값**.


## [3.27.0] — 2026-09-10

### Fixed — LESSONS 주입 실패가 "배운 게 없음"과 구별되지 않았다

`session-start.py` 의 `load_lessons` 가 `except Exception: return ""` 로 **완전히
조용했다.** 원장이 영구히 죽어도 *"아직 배운 게 없다"* 와 같은 결과라 아무도 몰랐다.
이제 실패에만 stderr 한 줄을 남긴다 — **fail-open 은 유지**(빈 문자열 반환, 세션을
막지 않는다)하되 **보이지 않게 하지는 않는다.** 원장이 그냥 없을 때는 여전히 조용하다
(상시 참 경고는 옆의 진짜 경고를 죽인다).

이 결함은 아래 §측정 8 을 쓰자마자 **그 규약을 이 레포에 적용해서** 나왔다.

**양성 대조를 두 번 만들었다.** 첫 프로브는 경고가 안 나와 *"수정이 안 먹었다"* 로 보였는데,
실제로는 **import 가 성공하고 원장이 그냥 없어서 정상 경로로 빠진 것**이었다 — 실패를
만들지 못한 프로브였다. `load_digest` 를 실제로 터뜨려 다시 재고서야 경고가 확인됐다.
오늘 같은 실수를 세 번째로 밟았다(§측정 3).

### Added — `warning-signal.md` §측정 7·8

**§7 — 두 관측을 나란히 놓기 전에 같은 것을 셌는지 확인한다.**
`[confirmed: 교차 세션]` 두 세션이 같은 도구의 실패 시각을 재서 값이 갈렸고, *"머신마다
다르다"* 로 정리한 뒤 **"머신 의존"** 이라는 인과까지 기록에 넣었다. 그런데 한쪽은
`duration_seconds`(스트림이 죽은 시각), 다른 쪽은 wall(프로세스 총 소요)이었다 —
**애초에 같은 것을 센 수가 아니었다.** 설명해야 할 차이가 없었다.
대가는 기록에 남은 잘못된 인과다: *"머신 의존"* 이라고 적혀 있으면 다음 사람은 재현이
안 될 때 **환경 차이를 쫓는다** — 실제로는 계측이 부실했던 것인데.
짝 규칙: **환경의 성질인지 대상의 성질인지는 두 번째 환경에서 재봐야만 갈린다.**

**§8 — 하위 계층이 실패를 삼켜 정상값으로 바꾸면 상위 카운터는 그것을 셀 수 없다.**
`[confirmed]` 외부 프로젝트의 분해기가 `consecutive_failures = 0` 인데 같은 레코드의
`outcome.reason` 에는 하위 호출의 예외가 적혀 있었다 — 카운터는 자기가 세도록 설계된
것을 **한 번도 본 적이 없었다.** 폴백을 설계할 때 *"이 폴백이 발화한 것을 세는 카운터가
그것을 볼 수 있는가"* 를 같이 묻는다. 못 보면 폴백은 보호가 아니라 **은폐**다.

### Changed — agy 관측 정정: 머신 차이는 **관측되지 않았다**

앞 커밋이 *"머신을 바꾸면 흩어진다"* 고 적었는데 **그 근거가 사라졌다.** 상대 세션이
동일 플래그로 전 필드를 남겨 재측정한 결과 두 머신이 **구별되지 않았다** — wall 은 양쪽
94~95s 고정, `duration_seconds` 는 양쪽 46~57s 산포. 이전의 "64~65s" 는 다른 플래그로
돈 실행이었다.

그래서 **wall ≫ duration 격차가 agy 의 성질로 승격된다** `[confirmed: n=5, 독립 머신 2대]`:
스트림은 46~57초에 죽는데 프로세스는 매번 타임아웃 만료까지 기다린다.
→ **실패 1건의 벽시계 비용 = 설정한 `--print-timeout` 값.**
타임아웃 상향은 성공률을 안 바꾸고 실패만 오래 붙잡는다.
하향(`60s`)은 이득일 수 있으나 **정상 응답이 60초 안에 끝난다는 전제를 측정할 수 없어**
(agy 가 한 번도 성공하지 않는다) 권고하지 않고 *"복구되면 가장 먼저 잴 것"* 으로 남긴다.


## [3.26.0] — 2026-09-10

### Fixed — 리뷰 계약의 완결성 가드가 **절단을 통과시켰다**

세 곳(`review-code`·`security-scan`·`skills/review`)이 *"빈 반환이 실패다"* 를 완결성
가드로 쓰고 있었다. **절단된 리포트는 비어 있지 않다.** 그리고 이 계약은 턴 예산 때문에
**부분 리포트를 의도적으로 허용**하므로, 잘린 리포트와 의도한 부분 보고가 **구별되지
않았다** — 호출자는 둘 다 정상으로 받았다.

이제 리포트는 반드시 `## 완료: 전체 …` 또는 `## 완료: 부분 — … 남은 것: …` 으로 맺는다.
그 줄이 없으면 호출자가 **잘린 것으로 취급**하고 배치를 더 쪼개 다시 호출한다.
**의도한 부분과 끊긴 출력을 가르는 것은 선언뿐이다.**

### Fixed — agy 레인 처방이 틀렸다 (스스로 같은 함정에 빠진 채 적었다)

바로 앞 커밋에서 *"rc 가 아니라 **출력의 존재**로 판정하라"* 고 적었는데 **그 가드로는
부족하다.** `agy` 의 타임아웃은 빈 출력이 아니라 `returning partial output` — **잘린**
출력을 내므로 '존재' 검사를 그대로 통과한다. 게다가 절단 사실은 **stderr 에만** 있어
`rc == 0` 경로에서 stderr 를 읽지 않는 호출자는 그 신호를 버린다.

처방을 완결성 기준으로 바꿨다: `--output-format json`(+`--json-schema`)으로 받아 **파싱
성공 + 기대 필드 존재**를 본다(잘린 JSON 은 파싱에서 죽으므로 **절단이 소리를 낸다**).
텍스트여야 하면 **종료 마커**를 요구한다. 그리고 **rc==0 이어도 stderr 를 읽는다.**

### Added — `warning-signal.md` §측정 6: '빈 출력'만 막는 가드는 '잘린 출력'을 통과시킨다

`[confirmed: n=2]`. 이 레포(위 리뷰 계약 3곳)와 외부 컨트롤 프로젝트(빈 출력을
`RuntimeError` 로 올리는 헬퍼가 있었는데, 절단은 잘린 stdout 이고 그 사실은 stderr 에만
있었으며 `rc != 0` 분기 안에서만 stderr 를 읽었다 → 잘린 응답이 성공으로 반환, 이미 발현).

두 번째가 배울 점이다 — **가드가 있었고, 작동했고, 그래서 안심했다.** 막은 것이 실제
실패 모드가 아니었을 뿐이다. 규칙 셋을 적었다: 완결성은 **구조로** 판정한다 ·
의도한 부분과 끊긴 출력을 가르는 것은 **선언**뿐이다 · **rc==0 이어도 stderr 를 읽어라.**

### 확인 — `pre-push` 의 자백은 정확하다 (실측)

교차 검토로 *"훅이 자백한 한계가 실제와 다를 수 있다"* 는 지적을 받아 우리 훅을 7케이스로
직접 돌렸다. python3 경로는 유효 JSON 6종(첫키·마지막키·minified·콜론 공백·개행 없음·
값이 다음 줄)을 **전부 차단**하고 깨진 JSON 만 fail-open — 의도대로다. 폴백(정규식) 경로도
**자백과 정확히 일치**한다: 키와 값이 다른 줄인 포맷만 통과, 나머지는 차단.

**첫 측정은 틀렸다.** `PATH` 를 벗겨 python3 를 숨기려 했더니 **전 케이스가 통과**로 나왔는데,
그건 폴백이 망가진 게 아니라 **훅이 더 앞에서 죽은 것**이었다(벗긴 PATH 에 훅이 쓰는
바이너리가 없었다). 함수만 격리해 다시 재고서야 실제 동작이 나왔다 — 측정이 대상을
건드렸는지 먼저 확인하라는 §측정 3 을 또 한 번 밟았다.


## [3.25.0] — 2026-09-10

전수 리뷰가 남긴 High 6건. 전부 **재현 후** 고쳤고, 결함마다 되돌려-FAIL 을 실측했다.
컨트롤이 병합 후 F-1·F-4 를 **독립적으로 재현**해 확인했다(워커 보고를 게이트로 쓰지 않는다).

### Fixed — `pre-push` 가 **JSON 키 순서**에 따라 통과시켰다 (F-1)

마커 스키마 검사가 줄 단위 `grep -E` 였다. `schema` 가 **마지막 키**인 pretty-print JSON
(`"schema": 1` 다음 줄에 `}`)이면 매치에 실패하고 `|| exit 0` 으로 **push 를 허용**했다.
훅이 자백한 한계는 *"키와 값이 서로 다른 줄"* 뿐인데, **키와 값이 같은 줄인데도** 뚫렸다.

컨트롤 독립 재현: 연결된 워크트리 + `schema` 마지막 키 마커 → 수정 전 통과, 수정 후
`rc=1` 로 차단되고 **원격에 아무것도 가지 않았다**(`fatal: Needed a single revision`).
이제 `python3` 이 있으면 JSON 파서로 판정하고, 없을 때만 정규식으로 폴백한다 —
같은 판정을 하는 `child-git-guard.py` 와 **두 벌로 갈려 다르게 판정하던 상태**를 닫았다.

### Fixed — conventions 소스가 **전면** 결손이면 green 이었다 (F-3)

한 파일만 없으면 red 인데 **전부 없으면 green** 이었다 — 방향이 거꾸로였다. `--check` 는
검사를 통째로 건너뛰고 `return 0`, write 는 **낡은 블록을 제거하지도 않았다.** 존재하지
않는 소스를 "자동 생성"이라 선언하는 블록이 소비자 AGENTS.md 에 영구히 남는 상태였다.
이제 **기존 블록이 있을 때만** red(소비자 레포는 애초에 그 블록이 없으므로 영향 없다).

### Fixed — 예산 게이트에 양성 대조가 없었다 (F-4)

게이트는 정상 동작하고 있었다. 결함은 **그 정합을 아무도 강제하지 않는다**는 것이었다 —
`load_rules` 의 신호 키 스키마가 갈리면 `signals` 가 전부 무시돼 `peak == always` 가 되는데,
0-파생 가드는 통과하므로 **과소측정으로 조용히 되돌아간다.** 이제 신호가 실제로 닿았음을
증명하지 못하면 red 다. `warning-signal.md` §측정 3 을 문서가 아니라 코드로 옮긴 것이다.

같은 파일이 에이전트 frontmatter 파싱 실패를 `continue` 로 건너뛰고 있었다 — 에이전트
하나가 깨지면 그만큼 예산에서 빠져 **더 쉽게 통과**했다. `SkipTally` 로 집계해 1건이라도
있으면 경로·사유·비율과 함께 red. 컨트롤 재현: 에이전트 하나의 frontmatter 를 깨뜨리자
`✗ 검사하지 못한 파일 1건 / 시도 32건 (3.1%)` 로 red.

### Fixed — `promote()` partial 이 이미 승격된 항목을 재승격했다 (F-2)

partial 분기가 원장을 미변경으로 두어, 다음 호출이 같은 항목을 다시 claim 하고 **성공했던
것을 또 호출**했다. frequency 가 부풀고 그 frequency 가 digest 순위를 정하므로 **진짜 반복
결함이 digest 밖으로 밀렸다** — 이관 순서를 뒤집어서까지 막으려던 바로 그 손해다.
이제 partial 에서도 성공분만 차감한다.

### Fixed — `promote()` 에 총량 데드라인이 없었다 (F-5)

항목당 타임아웃은 있었으나 총량이 없어 **최대 ~500초 블로킹**이 가능했다. 이 파일은 락에는
5초 데드라인을 걸어 두고("정지한 프로세스 때문에 파이프라인이 무한 대기하지 않게") 그보다
100배 긴 경로를 열어 뒀다. 총량 예산을 두고 초과분은 failures 로 계상한다.

### Fixed — 레지스트리 포인터에 소유권 검사가 없었다 (F-6, exec 경로)

`$(git-common-dir)/kit/registry.json` 은 untracked 인데 권한·소유자를 검사하지 않았고,
그 내용은 `subprocess.run` 으로 **실행**된다. 같은 파일의 `_lock_dir_is_safe` 가 훨씬 덜
위험한 락 디렉토리에 대해 정확히 그 검사를 하고 있었다 — *"이것이 박힐 수 있는데 대상이
아닌 곳"* 의 교과서적 사례다(`warning-signal.md` §5). 포인터와 부모 디렉토리 양쪽에
같은 검사를 적용하고 실패 시 폴백 + 경고.

### Changed — 차단 메시지가 명령 문자열 대신 실제 경로를 찍는다

`pre-push` 가 마커 위치를 `$(git rev-parse --git-dir)/kit/child.json` 이라는 **명령
문자열 그대로** 출력해, 사용자가 한 번 더 실행해야 위치를 알 수 있었다. 사실 진술에는
해석된 경로를 쓴다(해제 안내는 복붙용이므로 리터럴 유지).


## [3.24.0] — 2026-09-10

전수 리뷰(적대적 코드 리뷰 + 보안 스캔 + Codex·Antigravity 실물 프로브) 결과 배포 차단
2건을 포함해 6건을 고쳤다. 남은 항목은 후속 배치로 넘긴다.

### Fixed — 규범 2종이 생성물에서 **통째로 사라지고 있었다** (Critical)

`portable: true` + `tier: reference` 인 규범은 `_classify` 의 세 분기 **어디에도**
들어가지 않았다. 본문도 안 실리고, "이식된 룰" 표에도 없고, "이식 못 하는 것" 표에도
없었다 — 소비자의 `AGENTS.md`·`GEMINI.md` 에 **존재한다는 흔적조차** 없었다.

실측: `rules/child-marker`·`rules/delegation-contract` 가 생성물에 **0회** 등장.
하필 `child-marker` 는 v3.23.0 에서 추가한 **하네스 중립 `pre-push` 훅이 판정에 쓰는
마커의 스키마 SSOT** 다. 즉 **다른 하네스 워커가 읽어야 할 규범이 정확히 다른 하네스로
안 나가고 있었다.** 이 파일 자신의 설계 원칙(*"조용히 빠뜨리면 '내보냈다고 믿는데 안
나간' 구멍이 생긴다"*)을 이 함수가 어기고 있었다.

인스턴스가 아니라 **클래스로** 고쳤다: 버킷을 넷(`portable`/`index_only`/`not_portable`/
`unknown`)으로 나누고, **총합 불변식**(버킷 합 == 규범 수)을 `ClassificationError` 로
강제한다. 버킷을 하나 더 늘릴 때 같은 구멍이 다시 열리지 않게 막는 것은 새 분기가
아니라 그 한 줄이다. 생성물에는 「이름만 알리는 룰」 절이 새로 생긴다.

### Fixed — 원장을 **읽지 못했을 때 빈 원장으로 뭉개고 덮어썼다** (Critical)

`parse_ledger` 가 `except Exception: return []` 였다. 그래서 "파일 없음"과 "읽지 못함"이
같은 값이 되고, 그 직후 `upsert` 가 `_write_ledger` 로 파일을 통째로 교체해 **최대 50개
학습 항목이 stderr 한 줄 없이 사라졌다.** 발생 조건은 평범하다 — 손편집이 남긴 바이트
하나(`UnicodeDecodeError`), 공유 `.git/kit/` 를 다른 uid 가 먼저 만든 경우
(`PermissionError`), 병렬 실행의 `EMFILE`.

이 파일은 스스로 *"fail-open 은 '막지 않는다'이지 '지운다'가 아니다"* 라고 적어 놓고
그 원칙을 `promote()` 에만 적용하고 있었다. `LedgerUnreadable` 로 **부재와 실패를
분리**하고, 쓰기 경로(`upsert`·`promote` 차감·이관)는 읽기 실패 시 **쓰지 않고 물러선다**
(작업은 막지 않는다 — 배우지 못할 뿐이다). 읽기 전용 경로(`load_digest`)는 빈 문자열로
물러서되 stderr 에 남긴다.

### Fixed — 원장이 남이 심어 둔 심링크면 그 대상을 덮어썼다 (보안 스캔 M-1)

`_write_ledger` 가 `os.path.realpath` 로 링크를 따라가면서 **아무 검사도 하지 않았다.**
독스트링은 *"저장소 내부의 사용자 통제 파일이라 안전하다"* 고 적었는데 그건 **검사가
아니라 가정**이었다 — 같은 파일이 락 디렉토리에는 소유권 검사(`_lock_dir_is_safe`)를
걸어 두고 원장 경로에는 안 걸었다는 비대칭이 근거다.

심링크 **보존은 의도된 기능**(공유 원장 링크)이므로 봉쇄가 아니라 **소유권 확인**으로
갈랐다 — 내 소유 대상은 따라가고, 남의 것은 쓰지 않는다. 한계도 적었다: 같은 uid 로
도는 공격자는 통과한다(그 시나리오에서는 `.git/hooks/` 도 쓸 수 있으므로 이것이 마지막
방어선이 아니다).

### Fixed — 진입점이 잘릴 위험을 **소비자에게는 아무도 알려주지 않았다**

Codex 는 진입점들을 **병합한 총량**이 32 KiB 를 넘으면 경고 없이 잘라낸다. 잘리면 규범이
도착하지 않는데 도구는 `✓ 기록` 만 찍는다 — 이 킷이 존재하는 이유인 "약속 ≠ 실물"
결함이 정확히 그 형태다. 이 레포에는 같은 검사가 게이트(`§15`)로 있었지만 **그 게이트는
배포되지 않는다.** 이제 `export_harness.py` 가 직접 경고한다(fail-open — 크다는 이유로
exit 1 을 내면 규범이 **한 글자도** 안 가므로 잘릴 위험보다 나쁘다). 상한 값의 SSOT 는
`ENTRYPOINT_SOFT_CAP` 하나이고 `§15` 가 그것을 읽는다.

### Fixed — 생성물이 다른 하네스에 **틀린 수를 말하고 있었다**

이식 불가 표에 `(33종)` 이 리터럴로 박혀 있었고 실제는 32 였다. 이것은 이론이 아니다 —
**실제 Codex 세션에 "몇 종이라고 적혀 있나" 를 물었더니 `33` 이라고 답했다.** §11 드리프트
게이트는 **입력의 sha** 를 대조하므로 생성기 안의 리터럴은 볼 수 없다. 이제 센다.
셀 수 없으면(`agents/` 부재) **수를 적지 않는다** — "0종" 은 그 자체가 거짓말이다.

### Fixed — §21 파생에 제외 정책이 없어 정당한 파일 추가가 red 였다

v3.23.0 에서 훅 목록을 나열→파생으로 바꾸면서 **제외를 빼먹었다.** `git-hooks/` 에
`README.md` 하나만 생겨도 "마커 없는 킷 훅"으로 잡혀, 훅을 하나도 안 건드렸는데 게이트가
깨졌다. `warning-signal.md` §5 가 권하는 형태는 *"대상을 나열하지 말고 제외를 나열한다"*
이고, 제외 목록이 없으면 그 절반만 한 것이다. 제외를 **코드 상수**로 뒀다.

### Changed — `git ls-files` 결과의 디코딩을 명시 `[판정 불가: 이 플랫폼]`

git 쪽만 `-z` + `core.quotePath=false` 로 고치고 Python 쪽 디코딩을 미지정으로 두면
`text=True` 가 로케일 인코딩으로 strict 디코딩한다 — `LC_ALL=C` 인 CI 컨테이너에서
비-ASCII 파일명에 `UnicodeDecodeError` 가 난다는 지적이 있었다.

**이 플랫폼에서는 재현하지 못했다.** macOS 는 `LC_ALL=C` 여도
`locale.getpreferredencoding(False)` 가 UTF-8 이고, APFS 가 UTF-8 아닌 파일명 생성을
막아 양성 대조를 만들 수 없었다. 그래서 `[confirmed]` 가 아니라 `[판정 불가]` 로 적는다
(`warning-signal.md` §측정 3). 그럼에도 `encoding="utf-8", errors="surrogateescape"` 를
명시했다 — 파일시스템 경로 규약과 같은 처리이고, 암묵 로케일 의존은 그 자체가 결함이다.
`timeout=30` 도 같이 걸었다(이 레포의 다른 subprocess 호출은 전부 시한이 있는데 여기만
무기한이었다).

### Changed — 시크릿 마스킹 기준을 한 저장소 안에서 통일

`protect-sensitive.py` 는 매칭된 값의 앞 4자+뒤 2자를 디버그 로그에 찍었고,
`.private-names` 가드는 첫 글자+길이만 찍었다 — 한 레포에 기준이 둘이었다. 훅 출력은
터미널에 머물지 않고 트랜스크립트·CI 로그로 이동하므로(`warning-signal.md` §6)
**더 보수적인 쪽으로** 통일했다.

### 호환성 실측 (2026-09-10)

| 대상 | 결과 |
| --- | --- |
| Codex `codex-cli 0.153.4` | `AGENTS.md`(22.9 KB) **읽는다** — 오늘 추가한 운송 중립 문장을 원문으로 인용해 왔다 |
| Antigravity `agy 1.1.28` | `plugin validate` **통과**. skills 21종 전량 인식. agents 는 여전히 4(카테고리 디렉토리 수) — 중첩 레이아웃 미지원, 기존 판정 유지 |
| Gemini CLI `0.49.0` | 설치 확인. `GEMINI.md` 실도달은 **미측정**(쿼터) |
| Codex 훅 런타임 | **발화 확인** — SessionStart·UserPromptSubmit·Stop 이 실제로 돌았다. 단 전역 `~/.codex/hooks.json` 소속이며 **프로젝트 단위 훅은 여전히 미측정** |


## [3.23.0] — 2026-09-10

### Added — `pre-push` git 훅: 자식 워크트리의 push 를 **엔진 무관**으로 막는다

**무엇이 뚫려 있었나.** 자식 세션의 `git push` 차단은 `hooks/examples/child-git-guard.py`
에 있는데 그것은 **PreToolUse 훅**이라 Claude Code 하네스에서만 돈다. 실측: 다른
엔진(Codex)의 워커를 워크트리에 띄웠을 때 브리프의 *"push 금지"* 는 **산문일 뿐 기계
강제가 0** 이었다. 워커는 지켰지만 그건 모델이 지시를 따른 것이지 무엇이 막은 게 아니다.

**왜 git 훅이면 되는가 — 실측 `[confirmed]`:**

```
git rev-parse --git-dir         → .git/worktrees/<name>   ← 워크트리 고유
git rev-parse --git-path hooks  → <주 저장소>/.git/hooks   ← 공유
core.hooksPath                  → (unset)
```

연결된 워크트리는 `$GIT_COMMON_DIR/hooks` 를 **공유**한다. 주 저장소에 한 번 깔면 이후
생기는 **모든 워커 워크트리에 자동 적용**되고 누가 실행하든 똑같이 돈다. 동시에
`--git-dir` 은 워크트리마다 다르므로 **공유 훅 하나가 워크트리별로 다르게 판정**한다.
**공유 설치 + 국소 판정**이 동시에 성립하는 것이 이 설계가 성립하는 이유다.

판정은 `child-git-guard.py` 와 **정확히 같은 마커**(`$(git rev-parse --git-dir)/kit/child.json`)
를 본다 — 두 훅이 다른 것을 보면 그 자체가 결함이다. 탈출구는 `KIT_ALLOW_CHILD_PUSH=1`
이고 차단 메시지가 그 이름을 **직접 인쇄**한다(막기만 하고 푸는 법을 안 알려주는 훅은
사람이 훅 자체를 지운다).

**주 체크아웃은 마커가 있어도 통과한다** — 마커가 *없어서* 통과하는 게 아니라
`--git-dir == --git-common-dir` 로 **적극 판정**한다. "마커 부재에 기댄 통과"는 조용히
깨지는 자리라 테스트로 고정했다(`test_main_checkout_passes_even_with_marker`).

**정직한 한계 2건**(둘 다 훅 주석과 README 에 명시):

- git 훅은 **git 을 거치는 행위만** 본다. 파일 직접 수정·다른 워크트리 접근·API 호출은
  사정거리 밖이고 여전히 산문 규약이다. 중립 집행의 범위는 *"git 을 거치는 파괴적 행위"*
  로 한정된다 — 그 밖을 막는다고 주장하지 않는다.
- **`--dry-run` 은 탐지할 수 없다.** 실측: `git push --dry-run` 과 실제 push 는 훅에
  넘어오는 **argv·stdin·환경변수가 전부 동일**했다. 구분할 신호가 없으므로 dry-run 도
  함께 막힌다 — 추정으로 통과시키지 않는다(`warning-signal.md` "탐지할 수 없는 것은
  탐지할 수 없다고 적는다").

opt-in 이다. 설치·해제 방법은 훅 헤더와 `hooks/examples/README.md`.

### Fixed — §21 의 훅 목록이 나열이라 새 훅이 조용히 감시 밖이었다

`check_installed_hooks.py` 의 `KIT_HOOKS` 는 손으로 나열돼 있었고, 그 옆에
*"훅은 소수이고 각각 미설치 의미가 달라 제외 방식이 맞지 않는다"* 는 **정당화까지**
적혀 있었다. **바로 다음에 추가된 훅(`pre-push`)이 그 나열에서 빠졌다** — 등록되지
않은 훅의 드리프트는 영원히 잡히지 않는다. `warning-signal.md` §검토 절차 5 가 예측한
그대로이고, **나열을 정당화하는 주석을 쓴다고 나열이 안전해지지는 않는다**는 증거다.

정당화의 전제도 틀렸다. "각각 미설치 의미가 다르다"는 사람이 아는 지식이 아니라
**위치에서 파생되는 규칙**이었다 — `setup/pre-commit` 은 자동 설치라 없으면 알리고,
`setup/git-hooks/` 는 **opt-in 훅을 담으려고 존재하는 디렉토리**라 없는 것이 정상이다.

이제 파생한다. 새 훅은 그 디렉토리에 놓이는 것만으로 대상이 된다. 두 가지를 fail-closed
로 뒀다: **마커 없는 킷 훅 정본은 red**(대조 수단이 없으면 그 훅만 감시 밖이 되므로
조용히 건너뛰지 않는다) · **파생 결과 0건도 red**(파생 경로가 깨졌다는 뜻이다).
회귀 테스트가 거는 것은 "지금 훅 3종이 검사된다"가 아니라 **"새 훅이 디렉토리에 놓이는
것만으로 대상이 되는가"** 다 — 앞엣것은 훅을 추가할 때마다 낡지만 뒤엣것은 낡지 않는다.


## [3.22.0] — 2026-09-10

### Added — `cross-engine-review` 스킬: 서로 다른 엔진의 세션이 증거로 합의한다

**운송은 이미 가능했다 — 실측했다.** 다른 엔진(Codex, `codex-cli 0.153.4`, GPT-6 기반)의
세션을 별도 git 워크트리에 띄워 Claude 코디네이터가 구조화된 완료 보고를 받는 데 성공했다.
되는 이유는 오케스트레이터가 워커에게 주입하는 preamble 이 **전부 셸 명령**이기 때문이다 —
특정 하네스의 툴 호출이 하나도 없다. **셸이 있는 에이전트면 참여자가 될 수 있다.**

그래서 이 스킬이 정의하는 것은 운송이 아니라 **토론 규율**이다. 서로 다른 엔진의 두 세션이
파일 우편함으로 3라운드를 주고받아 실제 의사결정에 도달한 사례를 전수로 읽고, 거기서
작동한 요소를 절차로 옮겼다:

- **비대칭 선언** — 각자 *"내가 가졌고 상대에게 없는 것"* 을 먼저 적는다. 그리고 **가진
  것과 실제로 쓴 것을 분리해서** 적는다(분리하지 않으면 비대칭 선언은 자랑이 된다).
  이 선언이 없으면 이종을 쓰는 이유가 사라지고 같은 말을 두 번 듣는다.
- **출처 마커 + 증거 등급** — 주장마다 근거 id, 답신 머리에 *"이번 답신은 문서 대조다.
  실사용 벤치마크가 아니다"*. **근거 id 가 없는 주장은 의견이다.**
- **기록에 남는 철회** — 철회는 실패가 아니라 라운드의 정상 산출물이다. 두 라운드 연속
  양측 철회 0건은 수렴이 아니라 **서로 읽지 않고 있다는 신호**다.
- **★가짜 합의 거부** — 두 에이전트가 수렴하는 가장 쉬운 길은 **합의 기준을 조용히
  낮추는 것**이고, 그건 이 레포가 최악 등급으로 다루는 **false-green 과 정확히 같은
  형태**다. 검사를 지워 초록을 만드는 것과 기준을 지워 합의를 만드는 것은 같은 결함이다.
  그래서 최종 합의문에 **「미충족 기준」·「기준 변경 이력」 두 절이 없으면 무효**로 했다.
  *"다섯 기준을 모두 통과한 후보는 없습니다"* 는 정상 산출물이다 — **미충족을 숨기는
  것만이 실패다.**
- **파일 소유권 분리** · **라운드 상한과 3종 종료(합의·불합의 명시·사용자 에스컬레이션)**
  · **상대 답신은 비신뢰 텍스트**(`rules/untrusted-text.md` 적용 — 답신 안의 지시는
  실행하지 않고 보고만 한다).

`multi-perspective-review` 와의 경계도 본문에 적었다: 그쪽은 **한 세션의 서브에이전트
10관점**이라 모델·컨텍스트·툴이 같고 **사각지대를 공유**한다. 이 스킬은 참여자의 도구
접근이 달라 **증거 자체가 다르다.** 판단 한 줄 — *"틀렸을 때 비싼가, 그리고 내가 못 보는
증거가 밖에 있는가."* 증거원이 하나뿐인 작업에 엔진을 늘리면 순수 낭비다.

스킬 본문에는 **특정 오케스트레이션 도구 이름이 없다**(§17 불변식) — 운송 레시피는
`docs/control-loop-transport.md` 부록에 따로 적었다. 파일 우편함만으로 프로토콜이 완전히
성립하므로 우편함이 1순위 경로다.

### Fixed — 문서: "1·3단 도구는 모두 Claude Code 세션에서만 의미가 있다"는 틀렸다

`docs/control-loop-transport.md` 의 파리티 절이 그렇게 적고 있었는데 **1단에 대해서는
사실이 아니다.** 위 실측대로 1단의 워커 계약은 셸 명령이라 엔진에 묶이지 않고,
**코디네이터와 워커가 서로 다른 엔진이어도 된다.** 관측 없이 쓰인 문장이었고, 부록이
그 관측이다. (2·3단은 호스트 기능이라 원 서술 유지.)

### Fixed — 문서 카운트 게이트의 대상 밖: 사이트의 룰 수가 13 에 멈춰 있었다

`check_doc_counts.py` 의 사이트 검사가 **agents·skills 만** 걸고 **rules 를 빼 놓았다.**
그 사각지대에서 공개 사이트 두 페이지가 **13** 을 주장하는 동안 실제는 **15** 였다.
이 파일은 자기 주석에 같은 형태(§5 "대상을 나열하지 말고 제외를 나열한다")를 **두 번**
적어 두고도 목록 자신이 또 밟았다.

룰 패턴 4종(한국어/영어 × 어순 2종)을 추가했다. 추가하자마자 **내가 놓친 stale 2건을
더 잡았다** — 설명줄만 고치고 본문 불릿을 놓친 상태였는데, 그건 이 파일이 이미 문서화한
"설명줄이 본문 불릿을 가린다" 그대로였다. 되돌려-FAIL 로 판별력을 확인했다.


## [3.21.0] — 2026-09-10

### Fixed — 게이트를 파이프에 물리면 종료코드가 사라진다 (`definition-of-done` · `control-loop`)

파이프라인의 rc 는 **마지막 명령의 rc** 다. `gate | tail -25` 의 rc 는 `tail` 것이고
`tail` 은 거의 항상 0 이다 — **출력을 읽기 좋게 만들려던 습관이 게이트를 로그로 바꾼다.**

양성 대조까지 실측했다:

```bash
sh -c 'exit 7'            ; echo $?   # 7
sh -c 'exit 7' | tail -1  ; echo $?   # 0  ← 삼켜졌다
sh -c 'exit 7' | tail -1 && echo through   # "through" 가 찍힌다
bash -c 'set -o pipefail; sh -c "exit 7" | tail -1'; echo $?   # 7
```

`[confirmed: 2026-09-10, 두 세션 독립]` — 한 세션이 병합 게이트를 `--dry-run 2>&1 |
tail -25` 로 돌려 rc 0 을 통과 신호로 읽었다. 같은 날 **이 세션도** 완료 게이트를 파이프에
물려 돌리고 있었다. 인쇄된 `N pass / M fail` 줄을 읽었기에 v3.20.0 의 결론 자체는 맞았지만,
판정을 **사람이 읽는 텍스트**에 의존하고 있었다는 점은 같다. 그래서 릴리스 직후 rc 를 따로
잡아 재확인했다 — `./scripts/verify-done.sh > out 2>&1; echo $?` → **0**.

이 레포의 규범은 이미 같은 클래스를 둘 알고 있었다(판정이 stdout 에만 있는 검증 · 검증과
상태 변경을 한 호출에 잇는 것). **파이프가 세 번째 인스턴스**다. 셋 다 `&&` 를 통과시킨다.

- `rules/definition-of-done.md`: "rc 를 잃는 둘" 로 압축해 두 인스턴스를 나란히 적었다.
  §16 '항상' 축이 29B 초과로 **red 를 냈고**(새로 만든 축이 바로 다음 변경에서 작동했다),
  캡을 올리는 대신 같은 문단을 압축해 통과시켰다 — 10,163B / 10,240B.
- `skills/control-loop/SKILL.md`: 재현 스니펫과 실측, 그리고 처방을 적었다 — 게이트의 rc 를
  쓸 거면 파이프에 물리지 말고 **파일로 받고 rc 를 따로 잡아라**(`cmd > out 2>&1; rc=$?`).
  `set -o pipefail` 도 답이지만 **그 셸에서만 참이라 호출자가 잊으면 조용히 되돌아간다.**

rules 블록 sha256: `e503d55d82e1…`


## [3.20.0] — 2026-09-10

전수 적대적 리뷰에서 **실측된** 결함을 워크트리 워커 3인이 분담해 고친 배치다. 모든
전제는 재현 명령과 실제 출력으로 확인했고, 결함마다 되돌려-FAIL 을 실측했다.

### Fixed — `export_harness.py` 가 대상 트리 밖에 파일을 썼다 (Critical, 배포 차단)

`--entrypoints` 는 사용자 입력인데 `target_root / name` 은 **name 이 절대경로면
target_root 를 통째로 버린다**(pathlib 의 문서화된 동작). `_resolve_target()` 은
`if path.is_symlink():` 일 때만 봉쇄를 걸어 절대경로·`..` 는 검사 자체를 통과했다.

실측: `--entrypoints '/tmp/…/pwned.md' --target /tmp/…` 로 **트리 밖에 13,824 바이트가
실제로 기록**됐다. `--check` 경로에도 같은 구멍이 있어, CI 가 검사만 돌려도 트리 밖
파일을 읽었다(존재 여부·디코딩 오류가 오라클로 샌다).

`docs/conventions/path-containment.md` 에 기록된 **같은 결함 클래스의 네 번째
인스턴스**다. 그래서 새 봉쇄를 발명하지 않고 `scripts/build-targets.py::_resolve_in_repo`
와 **동일한 시그니처·계약**의 헬퍼로 교체했다 — 관례를 새로 발명하는 것이 이 결함이
네 번 반복된 이유다. 해석은 한 번뿐이고 그 결과를 읽기·쓰기 끝까지 쓴다(검사와 사용이
각자 resolve 하면 그 사이가 TOCTOU 창이다).

### Fixed — `docs/conventions/` 를 가진 소비자 레포에서 `AGENTS.md` 가 아예 안 써졌다 (Critical, 배포 차단)

`build_conventions_block()` 이 `conv_dir.is_dir()` 하나로 "kit 레포인가"를 판정했다.
자기 관례 문서를 가진 레포(= **모든** 소비자가 그럴 수 있다)에서는 등재 파일이 없어
`ClassificationError` → `main` 이 `return 1` → **진입점 루프에 들어가기 전에 종료**.
이 도구의 본래 목적인 규범 블록이 **한 글자도 써지지 않았다.** 바로 아래 루프가
*"하나가 실패해도 나머지를 건너뛰지 않는다"* 고 적어 놓고 정반대로 동작하던 지점이고,
consumer-first 북극성의 정면 위반이다.

판정을 둘로 나눴다: 등재 파일이 **하나도 없으면** 소비자 레포이므로 대상 아님(정상),
**일부만 있으면** kit 레포의 실제 드리프트이므로 red. 그 red 도 규범 블록 기록을
막지 않고 **전용 종료코드 3** 으로 구분한다(조용히 넘기지도 않는다).

### Fixed — `feedback_ledger.promote()` 가 읽지 않은 원장 항목을 지웠다 (Critical, 데이터 유실)

`promote()` 가 `_ledger_lock` **안에서** 항목마다 subprocess 를 돌렸다. 그런데 그 락은
5 초를 넘기면 경고만 찍고 **무락으로 진행**한다(의도된 fail-open). 따라서 promote 가
도는 동안 다른 프로세스의 `upsert` 는 락 없이 원장에 새 항목을 쓰는데, promote 는 자기가
**루프 시작 시** 읽은 목록만 알고 마지막에 `_write_ledger(path, [])` 로 **파일 전체를
비웠다** — 그 사이 항목은 승격되지도, 보존되지도 않고 사라졌다.

fail-open 은 *"막지 않는다"* 이지 *"지운다"* 가 아니다. 승격 호출을 락 밖으로 빼고,
전체 비우기를 **dedupe 키별 차집합 재계산**으로 바꿨다. `_LOCK_TIMEOUT_SECONDS` 의
fail-open 자체는 의도된 설계라 건드리지 않았다.

### Fixed — 구 원장 이관이 멱등하지 않고 실패를 침묵시켰다

`_write_ledger` 를 먼저, `legacy.rename(backup)` 을 나중에 했고 호출부가
`contextlib.suppress(Exception)` 으로 예외를 삼켰다. rename 이 실패하면 **매 SessionStart
마다 같은 항목이 다시 병합돼 frequency 가 부풀고**, 부푼 항목이 진짜 반복 결함을 digest
밖으로 밀어냈다. 개명을 먼저·병합쓰기를 나중으로 뒤집고, 실패에 stderr 경고 한 줄을
남겼다(fail-open 은 유지 — 학습 루프가 본 작업을 막지 않는다).

### Fixed — 락 디렉토리 소유권을 확인하지 않았다

`mkdir(mode=0o700, exist_ok=True)` 는 **이미 있는 디렉토리를 그대로 받아들이고**,
`mode=` 는 생성 시에만 적용된다. 공용 `/tmp` 에서 그 이름이 선점돼 있으면 남이 통제하는
디렉토리에 락 파일을 열었다(`O_NOFOLLOW` 는 파일의 심링크만 막지 디렉토리는 못 막는다).
`lstat` 으로 소유자·심링크 여부·group/other 쓰기 비트를 확인하고, 아니면 기존 폴백으로
내려간다.

### Fixed — 게이트 스크립트의 false-green 경로 4종

이 레포에서 false-green 은 **가장 나쁜 결함 등급**이다 — "통과했다"와 "돌지 않았다"가
집계에서 구분되지 않으면 게이트 전체의 신뢰가 무너진다.

- **검사 대상 0개가 초록이었다.** `check_old_names.py` 는 `previousNames` 가 비면 매치
  0건으로 exit 0 했다. 바로 옆 `check_shadowed_defs.py` 는 정반대로 *"검사 대상 0개는
  green 이 아니다"* 라며 exit 1 했다 — **같은 레포의 두 게이트가 같은 질문에 반대로
  답하고 있었다.** 후자의 정책으로 통일했다.
- **비-ASCII 파일명이 조용히 빠졌다.** `git ls-files` 를 `-z` 없이, `core.quotePath=false`
  없이 불렀다. git 기본값은 비-ASCII 경로를 따옴표+8진 이스케이프로 출력하므로 그
  문자열은 실제 경로가 아니고, 읽기가 실패하면 두 스크립트 모두 조용히 건너뛰었다.
  공용 헬퍼 `scripts/git_tracked.py` 로 묶었다.
- **읽기·파싱 실패가 집계되지 않았다.** "N개 파일"은 *시도한* 수였지 *파싱한* 수가
  아니었다 — 전부 못 읽어도 그 줄은 초록으로 나왔다. 이제 둘을 나눠 보고한다.
- **레지스트리 신선도 판정이 CWD 로 샜다.** `resolve_registry_repo` 가 None 을 줘도
  호출부의 `current_git_head(None)` 이 `cwd=None` 으로 **현재 디렉토리 레포의 HEAD** 를
  읽었다 — 엉뚱한 레포와 비교하고 통과시켰다.

### Changed — 주입 예산(§16)을 3축으로: 항상 / 최악 / 에이전트

`check_injection_budget.py` 가 `load_rules(PLUGIN_ROOT, False)` 를 불렀다 — `signals`
인자가 없어 `tier: conditional` 규범이 하나도 포함되지 않았고 결국 **core 만** 쟀다.
실제 세션은 신호를 켜서 부른다. **예산 게이트가 실제 주입량보다 적게 재고 통과시켰다.**

캡을 올리는 대신 축을 늘렸다:

| 축 | 묻는 것 | 상한 | 실측 |
| --- | --- | --- | --- |
| 규범(항상) | 모든 세션이 무조건 내는 비용 | 10 KiB | 10,069B |
| 규범(최악) | conditional 이 다 겹칠 때의 최대 | 22 KiB | 20,675B |
| 에이전트 | 하네스가 노출하는 목록 | 8 KiB | 7,837B |

'항상'만 재면 과소측정하고, '최악'만 재면 평범한 세션에 대해 과대보고해서 경고가
죽는다. conditional 신호 이름은 하드코딩하지 않고 `rules/*.md` frontmatter 에서
파생한다 — 나열하면 새 규범이 추가될 때 조용히 커버리지를 잃는다.

### Changed — `rules/parallel-worktree.md` 를 운송 중립으로 (block sha `c582f0f6e514…`)

주입되는 이 규범이 `isolation: worktree` + `ExitWorktree` 를 **유일한 격리 수단**인
것처럼 지목하고 있었다. 그 메커니즘을 훅으로 차단하고 외부 오케스트레이터를 정본으로
쓰는 레포가 실재하며, 그런 곳에서 이 규범은 "규범 없음"보다 나쁘다 — **없는 위임
수단이 있다고 오인하게 만든다.**

뿌리는 킷 내부 일관성 위반이다. 다른 주입 규범은 이미 전부 운송 중립이다
(feedback-loop·definition-of-done·planning-protocol·planning-check). 이것만
아웃라이어였고, 따라서 그 한 레포가 아니라 **`Agent isolation` 이 없는 모든 소비자
하네스**에서 같은 오인이 난다.

불변식(파일 소유권 disjoint · 임의 ours/theirs 금지 · 격리 트리 안 공유 상태 갱신
금지)만 규범에 남기고 구체 메커니즘은 "킷이 자기 에이전트를 저작할 때의 한 가지 구현"
으로 강등했다. 내용이 운송 중립이 된 이상 `portable: false` → `true` 로 바꿔
Codex·Gemini 진입점에도 같은 규범이 간다.

### Changed — 구/신 마커 공존을 이행 경로로 해석한다

구 토큰 블록과 신 토큰 블록이 **각각 하나씩** 공존하면 해석이 하나뿐이다 — 구 블록을
지우고 신 블록을 갱신하는 것. 그것을 "손상"으로 거부하면 **재생성으로도 못 고치는
영구 red** 가 된다(LESSONS 에 x3 로 등재된 패턴). 그 **외의** 손상(begin 만 있음,
3개 이상, 서로 맞물림)은 해석이 여럿이므로 계속 거부한다.

### Changed — 킷 이름·홈페이지를 매니페스트에서 파생 / eval 발췌 마스킹

`PREAMBLE`·`BLOCK_HEADER` 가 이름을 하드코딩하고 있었다(마커 토큰은 불변 — 바꾸면
기존 소비자의 `AGENTS.md` 가 전부 손상 판정된다). `evals/run.py` 의 실패 발췌는
인덱스 정렬을 고치고 시크릿 형태 문자열을 마스킹한다 — 훅·게이트의 출력은 터미널에
머물지 않는다(`warning-signal.md` §6).

### Changed — 문서: 드리프트 게이트 목록을 지우고 판정만 남긴다

CLAUDE.md 의 *"드리프트 게이트는 셋"* 문단이 게이트를 표로 열거했는데, 실제로는
일곱으로 늘어난 뒤에도 **셋에서 멈춘 채 낡아 있었다.** 같은 문단의 *"네 번째가
필요해지면 재검토한다"* 약속도 조용히 지나갔다. 이 레포는 바로 이 실패를 이미
`rules/definition-of-done.md`("열거하면 검사를 더할 때마다 낡는다")와
`warning-signal.md` §5("대상을 나열하지 말고 제외를 나열한다")에 적어 두었다 —
자기 규약을 자기 문서가 어긴 것이다. 통합 판정은 유지하고 열거만 지웠다.

`plugins/common/setup/git-hooks/` 의 존재와 "배포됐다 ≠ 설치돼서 돈다" 결함 클래스,
`/harness-export` 가 `GEMINI.md` 도 낸다는 사실도 채웠다(둘 다 CLAUDE.md grep 0건이었다).

`skills/review/SKILL.md` 에는 두 리뷰 에이전트가 `maxTurns: 10` 이라 한 번에 10여 개
파일을 넘기면 **리포트 없이 종료**한다는 실측을 호출 규약으로 적었다 — 빈 반환은
"결함 0건"과 구별되지 않는다.


## [3.19.0] — 2026-09-09

### Fixed — `git-workflow` 이 충돌 후 작업 트리를 되돌리지 않았다 (eval 이 찾아낸 첫 실제 결함)

에이전트가 병합 충돌을 **정확히 보고하고 사용자 결정을 기다렸으나**, `git merge --abort`
를 건너뛰고 **작업 트리를 충돌(MERGING) 상태로 남겼다.** 이 에이전트는 의도적으로
비격리(메인 워크스페이스에서 동작)라 그 상태가 남으면 **후속 git 명령이 전부 실패**한다.

**규범은 이미 최대 강도로 적혀 있었다**:

> **NEVER: 작업 트리를 충돌 상태로 남긴 채 종료하지 않는다.**
> 아래 순서를 그대로 따른다. **예외 없다** — *"사용자가 곧 답할 것 같다"* 는 판단으로
> 2단계를 건너뛰지 마라.

그런데도 안 지켜졌다. 발생 조건이 갈린다 `[confirmed]`:

| 실행 맥락 | 결과 |
| --- | --- |
| 시나리오 단독 | **0/4 실패** |
| 같은 에이전트 2 시나리오 | **2/3 실패** |
| 전체 38 시나리오(병렬·순차 둘 다) | **2/2 실패** |

단독으로만 재현했다면 "간헐적"으로 넘겼을 것이다. **실제 운영에 가까운 맥락일수록
일관되게 실패한다.**

### 산문이 아니라 구조를 바꿨다

지시를 더 세게 쓰는 것은 **이미 소진된 수단**이다(F-027: 프롬프트 규율에는 기계적 감지를
짝지어라). 원인은 강도가 아니라 **되돌리기가 출력에 흔적이 없는 동작**이라는 것이다 —
보고 형식에 그 항목이 없으니 건너뛰어도 보고가 완성된다.

그래서 **보이지 않던 동작을 출력 의무로 바꿨다**:

- 보고 템플릿에 **`### 작업 트리`** 항목 신설 — 생략은 형식 위반
- 절차에 **2-1) `git status --short` 확인** 추가, 그 출력을 그 항목에 그대로 쓴다
- *"비어 있지 않으면 아직 되돌리지 않은 것이므로 보고하기 전에 되돌린다"*

**재측정**: 같은 조건(에이전트 2 시나리오)에서 수정 전 1/3 통과 → **수정 후 3/3 통과**.
`[n=3, 표본이 작다 — 방향은 맞으나 통계적으로 확정은 아니다]` 다만 기전이 원칙적이라
(보이지 않는 동작 → 출력 의무) 우연 일치로 보지 않는다.

> **이것이 eval 투자의 회수 지점이다.** 17일 만에 돌린 전량 실행이 실제 결함 하나를
> 찾았고, 새로 넣은 출력 발췌가 원인을 한 번에 드러냈고, 구조적 수정 뒤 재측정으로
> 개선을 확인했다 — 킷이 설계한 학습 루프가 처음으로 끝까지 돌았다.


### Added — 릴리스 관례: `rules/` 를 바꿨으면 **블록 sha 를 CHANGELOG 에 적는다**

`plugins/` 무변경. `docs/conventions/release-process.md` 만.

진입점 블록을 자기 레포에 **정본으로 들여간 소비자**는 상류가 바뀐 것을 알 방법이 없다.
그쪽 드리프트 가드는 *자기 정본 ↔ 자기 라이브* 만 보고, 이 킷의 §11 은 *이 레포* 만 본다 —
**설계상 그렇게 뒀다**(소비자가 킷의 플러그인 캐시 경로에 의존하는 가드는 하네스 종속이
되고 조용히 사라진다). 그 대가가 **상류 변경 신호 부재**이고, 이 관례가 그 대가를 갚는다.

실측 계기: 소비 프로젝트가 **v3.16.0 export 를 채택**했고, v3.18.0 시점에도 바이트가
같은지는 **내가 손으로 대조해서야** 알았다. 그 대조가 없었으면 양쪽 다 모른 채로 갔다.

**현재 블록**: `rules-v1.4.0 sha256:49ae0be1d3d5041153a9e49f50c02d85d77440084eeaf344d7827960244e1258`
(v3.15.0 이후 불변 — v3.16.0·v3.17.0·v3.18.0 은 블록을 건드리지 않았다).

> **관례를 쓰다가 그 자리에서 오류를 하나 잡았다.** 초안은 값을
> `--stdout | shasum -a 256` 으로 얻으라고 적었는데 **다른 값이 나온다**(같은 시점에
> `3124d09d…` vs 마커 `49ae0be1…`). 마커의 sha 는 렌더된 블록이 아니라 **입력**
> (`rules-v`·헤더·PREAMBLE·룰별 이름+본문)을 덮기 때문이다. 소비자가 대조하는 것은
> 마커이므로 **마커에서 읽어야** 한다. 오늘 네 번째로 밟은 "쓰기 전에 확인" 사례다.


### Added — eval 리포트가 **실패 시 출력 발췌**를 남긴다 (2026-09-09, 레포 로컬)

`plugins/` 무변경. `evals/run.py` 만.

리포트가 판정만 남기고 출력을 버려서, 실패를 만나면 *"무엇이 출력됐길래 걸렸는지"* 를
알 수 없었다. 오늘 `output_not_contains` 실패 하나를 **수동 재현 3회로 못 잡았고**,
가드를 고칠지 에이전트를 고칠지 **추측할 수밖에 없는 상태**가 됐다. 추측으로 어서션을
약화시키면 그때부터 그 시나리오는 아무것도 안 잡는다 — false-green 을 만드는 지름길이다.

이제 실패한 검사의 `detail` 에 실린 값을 출력에서 찾아 **그 주변 3,000자**를 리포트에
남긴다(못 찾으면 앞부분). 통과한 실행은 남기지 않는다 — 진단 가치가 없고 리포트만 키운다.

### 관례 추가 — "측정은 그 자체가 오염될 수 있다" (`warning-signal.md`)

오늘 두 번 비싸게 배운 것을 관례로 올렸다 `[confirmed: n=2]`.

**① 픽스처로 통과한 것은 "동작한다"가 아니다.** 계약 프로브가 픽스처 42개를 통과하고
있었는데 전부 같은 레포 안이라 교차-레포 케이스가 한 번도 실행되지 않았다. *"있는
도구가 커버하니 새 검사가 불필요하다"* 는 결론도 같은 형태다 — 그 도구를 **실제
케이스로 돌려 본 뒤에만** 쓸 수 있다(`ruff F811` 이 밑줄 접두를 면제하는 것을
`def a()` 프로브로는 못 봤다).

**② 측정하면서 부하를 걸지 않는다.** 아래 항목이 그 사례다.

**③ 음성 결과는 "그 지점에 도달했다"를 따로 증명해야 한다.** 프로젝트 단위 훅 프로브가
캡처 0건을 냈고 *"지원되지 않는다 `[confirmed]`"* 로 적었는데, 로그를 보니 **세션이
시작조차 하지 않았다**(stdin 미차단). 0건은 "훅이 안 붙었다"가 아니라 "발화할 지점에
도달하지 않았다"였다. 기록을 `[판정 불가]` 로 정정했다.

셋 다 **같은 형태**다 — *측정이 실제로 대상을 건드렸는가* 를 묻지 않았다. 하루에 세 번
밟았기에 관례로 올린다. 규칙: 음성 결과에는 **양성 대조**를 같이 남긴다.

### eval 실행 2회 — 회귀 0건, 대신 **`--parallel` 유발 비결정성**을 찾았다

| 실행 | 조건 | 실패 |
| --- | --- | --- |
| 1차 | `--parallel 2`, **내가 같은 머신에 부하** | `implement-api` · `verify-code` |
| 2차 | `--parallel 2`, 부하 없음 | `git-workflow/merge-conflict-escalation` |
| 단독 재실행 | 각각 3~4회 | **전부 통과** |

**매번 다른 시나리오가 실패하고 단독으로는 전부 통과한다.** 오늘 변경(에이전트 32종·룰
3종 재작성)과 인과가 없다 — 인과가 있다면 같은 시나리오가 반복 실패해야 한다.
실행 로그도 `~/.claude.json projects 15 → 17 (+2) — 유령 등록 의심` 을 남겼다(동시
`claude` 프로세스가 같은 설정 파일에 쓴다).

**기준선(2026-09-04)도 `--parallel 2` 로 생성됐다.** 병렬이 비결정성을 유발한다면 그
기준선 자체가 운이었을 수 있다 — `--parallel 1` 전량 실행으로 확인한다.

### 발췌가 첫 실행에서 값을 했다

`git-workflow` 실패의 원인이 발췌 한 번으로 드러났다: 에이전트가 충돌을 **정확히 보고하고
사용자 결정을 기다렸으나 작업 트리를 충돌 상태로 남겼다.** 그런데 에이전트 정의는
*"NEVER 작업 트리를 충돌 상태로 남긴 채 종료하지 않는다 … 예외 없다"* 로 **이미 최대
강도로** 적혀 있다.

**규범이 도달했는데도 간헐적으로 안 지켜진다** — 산문 강조는 이미 소진된 수단이다(F-027:
프롬프트 규율에는 기계적 감지를 짝지어라). 여기서는 eval 어서션이 그 그물 역할을 했다.

### 측정 방법 교훈 — 부하를 걸면서 측정했다

전체 eval 을 `--parallel 2` 로 돌리는 **동안 같은 머신에서** codex 프로브·pytest·게이트를
계속 돌렸다. 그 실행에서 회귀 2건(`implement-api`·`verify-code`)이 잡혔는데 **둘 다 단독
재실행에서는 통과**한다. 기준선(2026-09-04)도 `--parallel 2` 로 38/38 이었다.

**측정과 부하를 분리하지 않으면 그 측정은 자기 자신을 오염시킨다.** 재측정은 다른 작업을
멈추고 돌린다.


## [3.18.0] — 2026-09-09

### Added — 세 번째 전달 형태(API 성 호출)를 배포물이 안내한다

규범이 도달하는 형태는 **셋**인데 킷은 **둘만** 문서화하고 있었다:

| 전달 형태 | 대상 | 경로 |
| --- | --- | --- |
| 세션형 + 훅 | Claude Code | SessionStart 훅이 자동 주입 |
| 세션형, 훅 없음 | Codex · Gemini CLI · OpenCode · Copilot · Cursor | `AGENTS.md` / `GEMINI.md` |
| **API 성 호출**(세션 아님) | 래퍼가 모델을 **함수처럼** 부르는 레인 | **`--stdout` 으로 뽑아 프롬프트에 싣는다** |

세 번째는 **진입점 파일이 개념적으로 없다** — 세션이 아니므로 하네스가 어떤 파일도
읽지 않는다. 규범은 **호출자가 프롬프트에 실어야만** 도달한다.

`--stdout` 은 이미 있었지만 *"블록 내용만 확인"* 이라는 **디버그 보조**로만 적혀 있었다.
있는 기능이 안 알려지면 없는 것과 같다 — 실제로 소비 측이 이 레인의 규범 공백을
그대로 두고 있었다.

**레인별 opt-in 과 전부/아무것도 규율도 같이 적었다**: 블록이 약 3.4k 토큰이라
분류·라우팅처럼 응답이 수백 토큰인 레인에 매번 실으면 비용이 본문을 압도한다.
그리고 티어별로 잘라도 절감이 10% 미만이며, 블록의 "정직한 한계" 표는 **훅이 아예 없는
이 레인에서 더 중요하다** — 토큰을 아끼려고 *"집행이 없다"* 는 사실을 지우면 모델이
집행이 있다고 오해한다.


### Fixed — 공개 README 의 하네스 표에 **한 번도 검사되지 않은 틀린 수치**가 있었다 (2026-09-09, 레포 로컬)

`plugins/` 무변경. 둘을 고쳤다.

**① `Rules (13)` — 실제는 15.** `check_doc_counts.py` 가 `rules \((\d+)\)` 를 **소문자로만**
찾아, 하네스 표의 대문자 `Rules (13)` 이 검사 대상 밖이었다. 같은 파일의 소문자 주장
(`rules (15)`)은 정상이라 **게이트가 계속 초록**이었다 — 검사 대상 밖에 결함이 쌓이는
`warning-signal.md` §검토 절차 5 형태 그대로다. 대소문자 무관 매칭으로 고쳤고,
되돌려-FAIL 로 확인했다(매치 2건을 전부 본다).

**② Antigravity 가 `rules/` 를 인식한다는 주장 — 거짓.** 오늘 실측했다: `rules/` 가
있을 때와 없을 때 `agy plugin validate` 출력이 **완전히 동일**하고, 그 도구가 세는
컴포넌트는 skills·agents·commands·mcpServers·hooks 다섯뿐이다. 표를
*"❌ not recognized — 규범은 진입점 파일로만 도달한다"* 로 정정했다.
(`packaging/targets.json` 은 앞서 정정했으나 **공개 README 는 그대로였다** — 같은 사실을
두 곳에 적으면 한쪽만 고치게 된다는 SSOT 교훈의 또 한 사례다.)


## [3.17.0] — 2026-09-09

### Fixed — 배포물이 **제거된 스킬을 아직 광고**하고 있었다

`agent-teams` 는 이 킷에서 제거됐는데(control-loop 로 대체) 참조가 다섯 곳 살아 있었다:

| 위치 | 무엇을 |
| --- | --- |
| `plugins/common/README.md` | `/agent-teams` 를 **사용 가능한 스킬로 표에 광고** |
| `README.md` | 같음 (공개 표면) |
| `CLAUDE.md` | 스킬 표에 등재 |
| `agents/meta/facilitator.md` | *"`skills/agent-teams/SKILL.md` 참고"* — **없는 파일을 모델에게 지시** |
| `skills/control-loop/SKILL.md` | "관련" 표가 같은 없는 파일을 가리킴 |

사용자가 `/agent-teams` 를 치면 아무것도 없고, `facilitator` 는 존재하지 않는 문서를
읽으라고 말한다. **"약속 ≠ 실물"** 이 배포 표면에 그대로 있었다.

### Added — §7 이 `skills/` 와 컴포넌트 상호참조를 보게 됐다

§7(stale 참조)이 `rules/` 와 `agents/` 만 봤다. **`skills/` 는 대상 밖이었고**, 위 죽은
참조 중 둘이 정확히 그 사각에 있었다. 오늘 기능 결함 2건(캐시 fallback glob)도 스킬의
경로 참조였다 — 같은 사각이다.

`warning-signal.md` §검토 절차 5("이 검사의 대상 밖은 어디인가")를 **§7 자신에게** 적용한
결과다. 넓힌 범위 둘:

- `scripts/*.sh` 참조 스캔에 `skills/` 추가
- **컴포넌트 상호참조**(`plugins/common/{skills,agents,rules,hooks}/…`)가 실재하는지 —
  제거된 컴포넌트를 가리키는 문서는 "없는 기능을 모델에게 안내하는 문서"다

**`tests/` 는 제외한다** — 오탐이 아니라 성질이 다르다. 테스트는 가짜 경로를 **일부러**
만들고(가짜 `git ls-files` 출력 등), 모델이 그것을 지시로 읽지 않는다. 실제로 이 제외가
없을 때 `hooks/new.py`(테스트 픽스처 문자열)가 잡혔다 — 오탐을 남기면 그 검사는 곧
무시당한다.

되돌려-FAIL 확인: 배포 산문에 없는 컴포넌트를 가리키게 하니 잡혔다.


## [3.16.1] — 2026-09-09

### Fixed — 원장 이관의 TOCTOU (같은 배치에서 만든 코드를 적대적으로 재검토해 찾았다)

v3.16.0 의 `migrate_legacy_ledger` 는 **존재 검사·`parse_ledger(legacy)`·개명이 모두
잠금 밖**이었다. 잠금을 기다리던 쪽이 **이미 읽어 둔 항목을 그대로 병합**해
frequency 가 두 배가 되고, 이어서 이미 개명된 파일을 또 개명하려다 죽는다
(그 예외는 `_try_migrate` 의 fail-open 이 삼키므로 **이중 계수만 조용히 남는다**).

검사와 상태 변경 사이의 틈이 곧 TOCTOU 다 — 이 레포가 `docs/conventions/path-containment.md`
에서 세 번 반복해 배운 그 형태를, 한 시간 전에 쓴 코드에서 또 만들었다.

**수정**: 재검사·병합·쓰기·개명을 **전부 잠금 안**으로. 잠금 밖 검사는 값싼 조기
탈출일 뿐 판정 근거가 아니라고 명시했다.

### 테스트를 두 번 버렸다 — 깨진 코드에서 통과하는 테스트는 false-green 이다

처음 쓴 회귀 테스트는 **스레드 둘을 띄우는 형태**였는데, 되돌려-FAIL 을 돌리니
**초판에서도 통과**했다. 스케줄러가 창을 벌려 주지 않으면 두 번째가 첫 번째 완료 뒤에
진입해 버린다 — **커버리지를 주장하면서 아무것도 안 잡는 테스트**다.

쓰기 지점에 배리어를 넣어 창을 벌리는 2차 시도도 같은 이유로 실패했다(두 번째가
`parse_ledger` 에 도달하는 시점을 통제할 수 없다).

3차에서 방식을 바꿨다 — 스케줄러에 기대지 않고 **경합의 결과 상태를 직접 만든다**:
잠금 진입 시점에 legacy 가 이미 개명돼 있는 상황. 이제 초판에서 **실제로 실패**한다.

> 남길 규율: **동시성 테스트는 되돌려-FAIL 로 검증하지 않으면 믿을 수 없다.** 통과하는
> 것만 보고 넣으면 그 테스트가 무엇을 잡는지 아무도 모른다 — 이 레포가 §22 에서
> false-green 을 red 로 다루는 것과 같은 이유다.


### 전임 킷 최종본이 v2.20.0 → **v2.21.0** (2026-09-09, 레포 로컬)

`plugins/` 무변경. `docs/marketplace-submission.md` 의 **현재형 주장 3곳**만 갱신했다.
날짜가 박힌 실측표(2026-09-08 카탈로그 조사)는 **그 시점의 사실이므로 그대로 둔다** —
과거 관측을 나중 사실로 덮어쓰면 실측이 아니라 서술이 된다.

전임 킷은 이 킷 v3.8.0 이 고친 **훅 설치 동결 결함**을 마지막 패치로 이식하고 닫혔다.
`.private-names` 비공개 이름 가드는 **이식되지 않았다** — 후속 킷 기능이고 전임 킷은
버그 수정만으로 닫는다는 판정이었다. 전임 킷을 참조할 일이 생기면 이 차이를 기억할 것.

### Added — §22 그림자 정의 게이트: **ruff 가 이것을 못 잡는다** (정정)

**앞서 이 CHANGELOG 에 "ruff F811 이 커버하므로 게이트가 불필요하다"고 적었다. 그 판정이
틀렸다.** 전임 킷 세션이 실측으로 뒤집었고, 내가 재현해 원인까지 특정했다.

내 프로브가 **실제 케이스와 달랐다.** ruff 는 `_` 로 시작하는 이름을
`dummy-variable-rgx` 로 "의도적 미사용"으로 보고 **F811 을 면제한다**:

| 이름 | 재정의 시 |
| --- | --- |
| `helper` | **F811 발화** |
| `_helper` | **침묵** |

**밑줄 접두는 이 레포 테스트 헬퍼의 기본 명명 관례**다(`_run_in_repo`·`_init_repo`·
`_fake_plugin_root` …). 면제 범위가 **정확히 위험 지대와 겹쳤다.** 실제 파일에
`_run_in_repo` 중복을 넣고 돌리면 `All checks passed!` 가 나온다 `[confirmed]`.

**왜 설정으로 안 고쳤나.** `dummy-variable-rgx = "^_$"` 로 좁히면 F811 이 잡는다.
그런데 그 설정은 F841·B007·RUF059 와 **공유**라 `_scenario`·`_lines` 같은 **서술적
폐기 이름 10곳이 `_` 한 글자로 강등**된다. 버리는 값이 무엇인지 이름으로 말하는 편이
낫다 — 그 가독성을 이 검사 하나와 바꾸지 않는다.

**왜 그래도 게이트를 만들었나.** 전임 킷 세션은 *"n=1 이니 만들지 말고 근거만 고쳐라"*
를 권했고 그 판단도 일리가 있다. 그런데 원인을 특정하고 보니 **누락이 우연이 아니라
명명 관례와 정렬돼 있다** — 밑줄 헬퍼를 파일 끝에 덧붙이는 한 계속 새는 구멍이다.
그리고 실패 형태가 **false-green** 이다: 테스트가 통과하면서 다른 것을 잰다.
이 레포가 가장 나쁘게 보는 것이고(F-012), 검사는 AST 12줄에 오탐 0건이다.

전수 확인: 추적 `.py` **91개, 그림자 정의 0건**. 되돌려-FAIL 로 실제 파일에 밑줄 헬퍼
중복을 넣으니 **ruff 는 통과시키고 §22 는 잡았다.**

> **정정에서 남길 것**: *"있는 도구가 커버한다"* 는 결론은 **그 도구를 실제 케이스로
> 돌려 본 뒤에만** 쓸 수 있다. 내 프로브는 `def a()` 두 개였고 실제는 밑줄 헬퍼였다 —
> 오늘 반복해서 만난 "픽스처가 실물과 다르다"의 또 한 사례다.

### 실행 순서 교훈은 유효하되, 이 사례의 증거는 아니다

*"싼 검사(린트)부터 돌리면 진단이 빨라진다"* 는 그대로 맞다. 다만 **이 사건에는
적용되지 않는다** — ruff 가 통과시키므로 순서를 지켰어도 못 잡았고, 결국 기준선
대조까지 갔을 것이다. 교훈과 그 교훈의 증거를 구분해 적는다.

## [3.16.0] — 2026-09-09

### Fixed — 학습 원장이 **워크트리마다 갈라지고 있었다**

`docs/works/feedback/ledger.md` 는 **작업 트리 안**이다. 워크트리마다 별도 파일이 된다.
그런데 이 킷은 워크트리 운영을 권장한다 — `isolation: worktree`,
`rules/parallel-worktree.md`, `control-loop` 전체가 그 형태다. **권장을 따르는 순간
학습 원장이 세션마다 갈라진다.**

실측: 같은 저장소의 두 워크트리에 **서로 다른 50항목 원장**이 있었다(sha256 불일치).
한쪽에서 오늘 배운 4건이 다른 쪽에 **영영 도달하지 않았다.**

`warning-signal.md` 가 기록한 것과 같은 형태다 — *"이 레포가 권장하는 사용 방식에서
조건이 참이 되면 그 장치는 죽는다."* 그때는 경고였고 이번엔 메모리였다.

**정본을 `<git-common-dir>/kit/ledger.md` 로 올렸다.** 모든 워크트리가 공유하고,
구조적으로 untracked 이며, git 만 있으면 되므로 하네스와도 무관하다 — 자식 마커·
레지스트리 포인터가 이미 사는 자리다. git 저장소가 아니면 구 위치로 물러선다(fail-open).

### 이관은 **이동이 아니라 병합**이다

워크트리마다 원장이 갈려 있었으므로 **먼저 도착한 하나만 채택하면 나머지의 학습이
사라진다.** 각 워크트리가 자기 것을 병합해 올린다 — dedupe 키가 같으면 frequency 합산,
최근 날짜 채택, id 재채번.

- 원본은 **지우지 않고** `.md.migrated` 로 **개명**한다. 지우지 않는 것은 되돌릴 수 있게,
  개명하는 것은 다음 실행이 또 병합해 frequency 를 부풀리지 않게(멱등).
- 심링크는 건드리지 않는다 — 사용자가 공유 원장으로 링크해 둔 경우다(F7).
- 이관 실패는 **본 작업을 막지 않는다**(fail-open) — 학습 루프의 원칙이다.
- 상한(`CAP=50`) 감쇠로 잘리는 항목이 있으면 **몇 건이 왜 빠졌는지 stderr 로 알린다.**
  상한은 설계된 동작이지만 이관 중에 일어나면 사용자가 고르지 않은 유실로 보인다 —
  조용히 지우지 않는다. 원본이 `.migrated` 로 남아 되돌릴 수 있다는 것까지 같이 알린다.

실측 검증: 두 워크트리의 원장을 병합해 **구 원장에만 있던 항목이 살아남는 것**과
**두 번 돌려도 frequency 가 안 부푸는 것**을 확인했다. 회귀 테스트 5종 + 되돌려-FAIL
(구 경로로 되돌리니 3종 실패).

> **이게 "메모리가 모든 툴에서 동일하게" 의 전제다.** 저장 위치가 워크트리마다 갈리면
> 어떤 하네스 통일도 그 위에서 성립하지 않는다.


### Fixed — 계약 프로브가 **다른 레포의 멀쩡한 파사드를 거짓 강등**시켰다 (2026-09-09, 레포 로컬)

`plugins/` 무변경. 소비 측이 `describe` 파사드 실물을 처음 내놓아 프로브를 돌렸고,
**그 과정에서 프로브 자신의 결함이 드러났다.**

`check_registry_describe.py` 가 `current_git_head()` 를 **인자 없이** 불렀다 — 즉
**프로브를 실행한 레포**의 HEAD 와 비교했다. 그런데 레지스트리는 보통 **다른 레포**에
있다(컨트롤 프로젝트가 자기 파사드를 내고 킷이 바깥에서 부른다). 두 레포의 HEAD 가
같을 리 없으므로 **항상 mismatch** 가 나고, D-49/D-50 은 지속 불일치를 "기동 커밋
파사드"로 보고 **강등**한다.

**cwd 파라미터는 이미 있었는데 아무도 넘기지 않았다** — 절반만 지어진 기능이었다.

**수정**: 포인터는 `<git-common-dir>/kit/registry.json` 에 사므로 **포인터 파일의 위치가
곧 그 레지스트리의 레포**다. 추측이 필요 없다. 커맨드를 직접 지정한 경우에는 `--repo`
로 받고, 그것도 없으면 **신선도 판정을 생략**한다 — 모르는 것을 CWD 로 때려 맞히지
않는다(그 순간 거짓 mismatch 가 되살아난다).

실물 대조 결과가 `[WARN] head 불일치` → `[PASS] head 일치` 로 바뀌었다.
회귀 테스트 3종 + 되돌려-FAIL 확인(구 동작으로 되돌리니 2종 실패).

> **교훈**: 계약을 검증하는 도구도 **실물에 닿기 전에는 검증되지 않는다.** 이 프로브는
> 픽스처로 42개 테스트를 통과하고 있었지만, 그 픽스처가 전부 같은 레포 안에 있어서
> 교차-레포 케이스가 **한 번도 실행되지 않았다** — `warning-signal.md` §검토 절차 4
> ("이 검사가 도는 조건")의 다른 얼굴이다.


## [3.15.0] — 2026-09-09

### Fixed — 이식되는 룰 2종이 더 Claude 전용 도구를 지시하고 있었다

v3.14.0 에서 `feedback-loop` 하나를 고친 뒤 **클래스를 물었다**(`warning-signal.md`
§검토 절차 5: "인스턴스를 고칠 때 클래스를 묻는다"). 이식 대상 룰 전체를 Claude 전용
프리미티브로 훑으니 둘이 더 나왔다 — **둘 다 `core` 티어**라 매 세션 주입되면서 동시에
다른 하네스로 이식되고 있었다:

| 룰 | 지시하던 것 | 그 하네스에서 |
| --- | --- | --- |
| `definition-of-done` | `TaskList` 를 조회하라 | 그 도구가 없다 → 규율이 무동작 |
| `loop-engineering` | `TaskUpdate(completed)` 를 호출하라 | 같음 |

**규율 자체는 하네스 무관이다** — *"보고보다 마킹이 먼저"*, *"완료를 기록하고 다음으로"*.
도구 이름만 종속이었다. 그래서 이 레포에 **이미 있던 관용구**로 바꿨다:
`호스트가 제공하는 수단으로` — `planning-check`·`planning-protocol`·
`agent-delegation-chain` 이 쓰던 표현이다. 새 표현을 발명하지 않았다.

`loop-engineering` 의 `auto-dev` 참조도 `배치 실행(킷의 auto-dev 등)` 으로 일반화했다 —
스킬 호출 형태가 하네스마다 다르다.

**전수 확인**: 이식 대상 11종에 Claude 전용 참조 **0건** `[confirmed]`.

**예산**: 상시 주입 9,986B → **10,069B** (+83B, 캡 10,240B). 처음엔 예시를 괄호로
달아 +145B 였는데(여유 109B), 기존 관용구가 예시 없이 쓰는 형태라 맞췄다 — 일관성이
바이트도 돌려줬다.


## [3.14.0] — 2026-09-09

### Fixed — `feedback-loop` 은 `portable: true` 인데 다른 하네스에서 **영원히 발동하지 않았다**

이식 대상으로 내보내면서 발동 조건이 *"session-start 가 `=== LESSONS ===` 를 주입하면"*
이었다. 그 주입은 **Claude Code 훅만** 한다 — Codex·Gemini·API 성 호출에서는 조건이
**한 번도 참이 되지 않는다.** 게다가 `implement-code`·`review-code` 같은 **Claude 전용
에이전트 이름**을 참조했다.

**"이식했다"고 적어 놓고 실물은 무동작**이었다 — 이 킷이 반복해서 잡아온 결함 클래스가
규범 자신에게 있었다. `warning-signal.md` §검토 절차 4("이 검사가 도는 조건을 한 문장으로
쓸 수 있는가")를 **룰에 적용하면** 바로 드러난다: *"훅이 LESSONS 를 주입했을 때"* 라고
쓰는 순간 훅 없는 하네스에서 안 돈다는 게 보인다.

**저장은 이미 중립이었고 전달만 종속이었다.** `feedback_ledger.py digest` 는 하네스와
무관한 CLI 다. 그래서 룰을 **얻는 방법이 하네스마다 다르고 규율은 같다**로 다시 썼다:

| 하네스 | digest 확보 |
| --- | --- |
| 훅 있음(Claude Code) | 자동 주입 |
| 훅 없음(Codex·Gemini·CLI) | **직접 조회** — `feedback_ledger.py digest` |
| API 성 호출(세션 아님) | **호출자가 미리 조회해 프롬프트에 싣는다** |

한 줄을 명시적으로 박았다: **"주입을 못 받았으니 해당 없음으로 넘어가지 마라 —
조회 수단이 있으면 조회한다."** 이게 없으면 모델이 조건 불충족으로 읽고 넘어간다.

에이전트 이름 참조는 **역할 서술**로 바꿨다(구현 시 / 리뷰 시) — 서브에이전트가 없는
하네스에서도 읽힌다.

**대가**: 이식 블록이 12,964B → 13,522B (**+558B ≈ +139 tok, +4.3%**). 3개 하네스
형태 중 3개에서 무동작이던 규율이 동작하게 되는 값이다.

**남은 한계(적어 둔다)**: 훅 없는 하네스가 `feedback_ledger.py` **경로를 스스로 찾아야**
한다. 하네스마다 플러그인 캐시 위치가 달라 룰에 경로를 박을 수 없다 — 못 찾으면
무동작(fail-open)이고 룰이 그렇게 말한다. 경로 발견을 계약으로 만드는 것은
`registry describe` 이음매가 닫힐 때 함께 다룬다.


## [3.13.0] — 2026-09-09

### Fixed — 규범 블록이 같은 룰을 두 번 실었다 (SSOT 위반, 매 export −840B)

`untrusted-text` 규범이 블록에 **두 번** 들어갔다: 헤더의 요약본(840B) + 원문(1,434B).
번호 항목 셋이 **한 글자도 다르지 않은** 중복이었다.

두 가지가 동시에 잘못됐다:

- **`rules/ssot.md` 위반** — 같은 규범이 두 곳에 있으면 한쪽만 고칠 때 조용히 갈린다
- **블록 자신의 정책과 모순** — 이 블록은 "원문 그대로 / 요약 금지"를 내걸고 있는데
  헤더에 요약본이 있었다

**요약본을 넣은 의도는 정당했다** — 그 룰 자신이 *"방어 프레이밍을 페이로드보다 먼저
선치하라"* 고 규정하므로 규범 묶음 안에서도 먼저 와야 한다. 그런데 룰이 파일명 순서로
실려 `untrusted-text` 가 **맨 뒤**에 놓였고, 그 순서를 못 고쳐서 요약본을 앞에 하나 더
둔 것이다.

**중복 대신 순서로 해결했다** — `untrusted-text` 를 정렬 키로 맨 앞에 올린다.
한 번만, 원문으로, 맨 앞에.

### 규범 블록 실측 (API 성 호출 레인의 토큰 예산 입력값)

소비 측이 "규범을 프롬프트에 실을 때 얼마인가"를 물어 실측했다. `--stdout` 기준:

| 구간 | 크기 | 비중 |
| --- | --- | --- |
| **전체** | **12,964B ≈ 3,241 tok** | 100% |
| 스캐폴딩(마커·헤더·워크플로·이식표·**한계표**) | 2,727B ≈ 681 tok | 21% |
| 규범 원문 8종 | 10,237B ≈ 2,559 tok | 79% |

**티어별 절단 플래그는 만들지 않는다.** 이식 대상이 core 7종 + conditional 1종인데
conditional 을 빼도 **8.1%** 밖에 안 준다 — 유지·테스트해야 하는 옵션 하나를 늘릴
값어치가 없다. 이번에 없앤 중복(6%)이 플래그 하나보다 컸다.

**스캐폴딩 절단(`--bare`)도 만들지 않는다.** 21% 로 크지만, 그 안의 "정직한 한계" 표는
*"다른 하네스에서 이 규범은 강제 장치가 아니라 규율 문서로 동작한다"* 를 말한다 —
훅이 아예 없는 API 성 호출 레인에서는 **더 중요한** 정보다. 지우면 모델이 집행이
있다고 오해한다.

그래서 레인 결정은 **티어가 아니라 전부/아무것도**다. 규범이 필요한 레인(코드 생성·결정
산출)에는 3,241 tok 을 싣고, 분류·라우팅 레인에는 싣지 않는다. 캐시 + sha 로 갱신한다.


### Fixed — `targets.json` 이 agy 의 `rules` 인식을 근거 없이 주장했다 (2026-09-09, 레포 로컬)

`plugins/` 무변경. Antigravity 타겟의 `recognizedDirs` 가 `["skills","rules"]` 였는데
**`rules` 는 사실이 아니다** `[confirmed: agy 1.1.27]`. 스크래치 플러그인에 `rules/` 를
두었을 때와 지웠을 때 `agy plugin validate` 출력이 **완전히 동일**했고, 그 도구가 세는
컴포넌트는 skills·agents·commands·mcpServers·hooks **다섯뿐**이다.

파급: **agy 로 가는 규범 경로는 플러그인이 아니라 진입점 파일뿐이다.** 그리고 agy 가
API 성 호출로 쓰이는 레인(세션이 아니라 함수 호출)에서는 진입점 파일조차 개념적으로
없으므로, 그 경우 규범은 **호출자가 프롬프트에 실어야 한다** —
`export_harness.py --stdout` 이 마커 없이 규범 블록만 표준출력으로 낸다.

에이전트 오집계는 1.1.20 → 1.1.27 에서 그대로다(카테고리 4개를 4로 세고 실제 32종 인식 0).

### 규범 도달을 실제로 관측할 수단이 없다 (한계 기록)

세 하네스가 **어느 진입점을 실제로 읽는지** 마커 프로브로 재려 했으나 **측정하지 못했다**:

| 하네스 | 결과 |
| --- | --- |
| agy 1.1.27 | **쿼터 소진** — "Individual quota reached, resets in 36h" |
| gemini-cli | API 400 (이 환경의 인증·설정 문제) |
| codex 0.153.4 | 응답 없음(장시간) |

**그래서 "규범을 이식했다"는 지금도 미검증이다.** 파일을 만들었고 sha256 으로 드리프트를
막고 있을 뿐, 그 파일을 하네스가 **실제로 읽는지는 관측한 적이 없다.** 소비 측 컨트롤
세션도 같은 상태임을 확인했다 `[confirmed: 관측 장치 부재]`. 없는 검증을 있다고 적지
않는다(`docs/conventions/warning-signal.md`) — 관측 장치는 별도 작업으로 남긴다.


## [3.12.1] — 2026-09-09

### Fixed — Codex 매니페스트의 `displayName` 이 구 제품명이었다

`packaging/targets.json` 의 `interface.displayName` 이 **`"Claude Code Kit"`** 이었고,
그대로 `.codex-plugin/plugin.json` 에 실려 나갔다 — **Codex 사용자에게 표시되는 이름**이다.

§20 이 못 잡은 이유: `previousNames` 에 하이픈 형태(`claude-code-kit`)·약칭(`CCK`·`cck`)만
있었고 **공백·타이틀케이스 형태(`Claude Code Kit`)가 없었다.** 개명 잔재는 정식 이름
그대로만 남는 것이 아니라 **표기 변형**으로도 남는다 — v3.9.0 에서 약칭·경로 이름을
넣으며 같은 교훈을 얻었는데 표기 변형 축을 빠뜨렸다.

`previousNames` 에 추가했다. 이제 넷을 본다: `claude-code-kit` · `Claude Code Kit` ·
`CCK` · `cck`.

> **왜 이게 게이트로만 잡히는가**: 사람이 읽어도 "Claude Code Kit" 은 자연스러운 문구라
> 눈에 안 걸린다. 실제로 v3.8.0 의 전수 정리에서도, v3.9.0 의 약칭 정리에서도 살아남았고,
> 사용자가 *"codex·agy 호환이 실제로 되느냐"* 고 물어 **생성물을 직접 열어 보고서야**
> 드러났다. 리터럴 목록 검사가 사람의 훑어보기보다 강한 이유다.


## [3.12.0] — 2026-09-09

**보이지 않던 상시 비용 축을 예산에 넣었다.** §16 은 "세션 주입 예산"을 표방하면서
한쪽 축만 재고 있었다 — `warning-signal.md` §검토 절차 5("이 검사의 대상 밖은
어디인가")가 가리키던 자리다.

### Removed — `facilitator-teams` 에이전트 (실행 불가능한 상태로 상시 비용만 냈다)

제거된 **Agent Teams 모드**(`spawnTeam()` 실험 경로)의 Lead 였다. 그 모드는 이 킷에
없는데 에이전트만 남아 **매 세션 470B** 를 쓰고 있었다 — 32종 중 **최대 항목**이었다.

이 킷이 이미 답을 적어 뒀다. `evals/policy.json` 의 분류가 등급 **C**, 사유가
*"spawnTeam() 의 Lead 로만 존재하며 살아있는 teammate 없이는 **단독 실행 자체가 불가**"*
였다. 실행할 수 없다고 기록된 에이전트가 모든 세션의 컨텍스트를 먹고 있었다.

함께 걷어낸 것: `multi-perspective-review` 스킬의 **Agent Teams 모드 절 전체**(1,638B).
`CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`·`spawnTeam()`·모드 자동선택·Teams↔Subagent
폴백을 설명하고 있었다 — **없는 기능을 모델에게 시도하라고 지시하는 문서**다. 모드와
무관하게 유효한 토큰 한도 규율만 남겼다. 메타 에이전트 4종의 참조 문단도 제거했다.

에이전트 33 → **32종**. 문서 카운트 6곳(README·plugins/common/README·CLAUDE.md·site 2종)
동기화.

### Added — §16 에 두 번째 축: 에이전트 설명 예산 (8 KiB)

상시 비용의 출처가 둘인데 **한쪽만 재고 있었다**:

| 축 | 출처 | 줄이는 방법 | 현재 |
| --- | --- | --- | --- |
| 규범 + WORKFLOW | 이 킷의 SessionStart 훅 | core 티어 축약 / 티어 강등 | 9,986B ≤ 10,240B |
| **에이전트 설명** | **하네스**가 선택용으로 노출 | 에이전트 수 / description 길이 | **7,837B ≤ 8,192B** |

두 번째 축은 **`claude plugin details` 의 `Always-on` 추정에도 안 잡힌다** — 그 명령은
`agents/*.md` 만 세고 하위 디렉토리를 무시하는데(v3.11.x 에서 실측) 이 킷의 에이전트는
전부 `agents/{category}/` 에 있다. 도구가 못 세는 것을 우리가 대신 센다.

**축을 합치지 않았다.** 규범 축은 합산이 옳다 — 절을 파일 사이로 옮기는 것만으로
통과시키는 게이밍이 가능하기 때문이다(실측). 반면 에이전트 축은 재료도 줄이는 수단도
다르다. 합치면 **"규범을 깎아 에이전트를 늘리는" 교환이 조용히 통과**하는데, 그 둘은
교환 가능한 자원이 아니다.

**상한은 깎은 뒤에 정했다(D-46).** 죽은 에이전트를 걷어낸 **뒤** 7,837B 에서
8,192B(8 KiB)로 잡았다. 여유 355B 는 **평균 에이전트 1개분**이라 에이전트를 더할 때마다
"매 세션 이 비용을 낼 값어치가 있는가"를 묻게 된다. 규범 축의 여유율(4.0%)과 같은 압력이다.

### Fixed — §16 이 CI 에 없었다

판정이 `verify-done.sh` 안 **인라인**이라 "누가 로컬에서 게이트를 돌릴 때만" 발화했다.
§18 이 겪은 것과 같은 구멍이다. `scripts/check_injection_budget.py` 로 빼서
verify-done.sh 와 CI 가 **같은 스크립트**를 호출한다(F-023).

되돌려-FAIL 확인: description 을 부풀리니 초과를 잡고 **어느 에이전트가 큰지까지** 보고했다.


## [3.11.2] — 2026-09-09

### Changed — 드리프트 마커의 계약을 훅 본문에 명시

소비 측에서 질문이 왔다: §21 의 마커 판별이 **"2행 정확히"인가 "첫 N행 안 어디든"인가.**
자기 레포에 정본으로 들이면서 출처 주석 한 줄을 위에 끼워 마커가 3행이 됐기 때문이다.

**답: 위치 무관이다** — 고정 문자열 검색(`grep -F`)이라 파일 어디에 있어도 된다.
3행·10행·마지막 행에 두고 전부 실측했다 `[confirmed]`. 세 경우 다 "마커 없음" 분기로
빠지지 않고 내용 비교 단계까지 갔다.

**질문이 나왔다는 것 자체가 결함 신호다.** 마커의 계약이 코드에만 있고 훅 본문에 없었다.
"2행 고정"으로 오해한 채 출처 주석을 끼웠다면 **드리프트 감시가 조용히 꺼졌을** 것이고,
꺼졌다는 사실은 아무 데서도 드러나지 않는다. 그래서 계약 셋을 훅 본문에 적었다:

1. 판별은 **위치 무관**
2. 마커가 **없으면 대조 자체를 하지 않는다** — 지우면 감시가 조용히 꺼진다
3. 제품명을 넣지 않는다 — 개명 때 마커가 깨지면 대조가 멈춘다

그리고 **경계를 하나 더 적었다**: `verify-done.sh §21` 은 **레포 로컬 게이트라 배포되지
않는다.** 마커는 그 게이트 전용 장치가 아니라 **누구의 드리프트 가드든 쓸 수 있는 규약**
이다. 훅을 자기 레포 정본으로 들이는 쪽은 대조 기준을 **킷의 파일이 아니라 자기 레포의
정본**으로 잡아야 한다 — 출처 주석 한 줄만 더해도 바이트가 갈린다.

계약을 **테스트로도 고정했다**. 산문만 있으면 다음 사람이 위치 고정 판별로 바꿔도 아무것도
잡지 못한다.


## [3.11.1] — 2026-09-09

### Fixed — §21 이 opt-in git 훅을 안 보고 있었다 (`§검토 절차 5` 자기적용)

v3.11.0 이 `reference-transaction` 훅을 들이면서 소비자에게 **손으로 `cp` 하라**고
안내했다. 그건 바로 앞 릴리스에서 고친 **install-once/드리프트 문제 그 자체**다 —
복사한 뒤 킷이 훅을 고치면 소비자 쪽은 영원히 옛 판이고, 아무것도 그것을 알리지 않는다.
당시 CHANGELOG 에 "§21 은 이 훅을 감시하지 않는다"고 **한계로 적어만 뒀는데**,
적을 수 있으면 대개 덮을 수도 있다.

**§21 을 목록으로 바꿨다.** 새 킷 훅이 생기면 한 줄만 더한다 —
`warning-signal.md` §검토 절차 5("이 검사의 대상 밖은 어디인가", "대상이 아니라 제외를
나열한다")를 검사 자신에게 적용한 것이다.

**설치 여부의 의미가 훅마다 다르다는 점을 반영했다**:

| 훅 | 미설치일 때 | 왜 |
| --- | --- | --- |
| `pre-commit` | 노란 줄로 **알린다** | session-check 가 자동 설치한다 — 없으면 집행이 안 도는 것이다 |
| `reference-transaction` | **침묵**(정상으로 본다) | opt-in 이다 — 안 켠 것을 결함으로 보고하면 켜지 않은 모든 소비자에게 상시 참이 되어 그 경고가 죽는다 |

훅 소스에 중립 마커 `# kit-managed-hook` 을 넣었다. 마커가 없으면 소비자 소유로 보고
대조하지 않는다. **제품명을 넣지 않았다** — 개명 때 마커가 깨지면 대조가 조용히 멈춘다.
경로 이름에서 제품명을 뺀 것(v3.9.0)과 같은 이유다.


## [3.11.0] — 2026-09-09

### Added — 하네스 중립 집행의 첫 실증: `setup/git-hooks/reference-transaction`

**질문은 이랬다**: Claude Code 의 PreToolUse 로만 막던 규율을 하네스 무관하게 집행할
수단이 있는가. `git stash` 는 커밋을 만들지 않아 pre-commit 이 볼 순간이 없고, git
alias 는 builtin 을 가로채지 못한다. **답: `reference-transaction` 훅이 있다** —
모든 ref 갱신에 발화하므로 `refs/stash` 도 잡힌다(git 2.28+).

실측으로 확인한 것 `[confirmed]`:

| 항목 | 결과 |
| --- | --- |
| bare `git stash` | **차단** (`fatal: ref updates aborted by hook`) |
| `git stash push -u -m "tag"` | 통과 |
| 차단 시 작업트리 | **완전 보존** — 수정·untracked 파일 그대로, stash 0개 |
| `pop`/`drop` (삭제 방향) | 막지 않음 |

**판정을 문구가 아니라 데이터로 한다.** bare stash 는 제목을 `WIP on <branch>:
<HEAD 한 줄>` 로 자동 생성하는데, `"WIP on"` 접두어로 판정하면 **로케일 번역에 깨질
수 있고 그 방향이 "조용히 통과"**다 — 가드가 죽는 쪽이다. 그래서 번역되지 않는 부분,
**제목이 HEAD 의 `%h %s` 로 끝나는가**로 판정한다. 경계 사례(`-m` 에 HEAD 한 줄을
그대로 쓰는 경우)는 **차단 방향으로 실패**하므로 안전하다.

> 로케일 독립성은 **직접 실측하지 못했다** — 이 환경의 git 빌드에 번역 카탈로그가
> 없어 `ko_KR` 에서도 영어가 나왔다. 그래서 "문구가 번역되지 않는다"에 기대지 않고
> 애초에 문구를 안 보는 설계를 골랐다. 검증할 수 없는 전제 위에 가드를 세우지 않는다.

회귀 테스트 5종 + 되돌려-FAIL 확인(무동작 훅으로 바꾸면 3종이 실패).
**opt-in 이다** — `hooks.json` 에 없고 문서화된 복사 한 줄로 켠다.


### Added — §21: 설치된 훅이 repo 정본과 같은가 (2026-09-09, 레포 로컬)

`plugins/` 무변경. `scripts/verify-done.sh` 만.

**이 킷의 대표 결함 클래스를 게이트로 내렸다** — *"커밋·테스트·문서를 다 갖춘 가드가
설치본에 없어 집행이 0회"*. v3.7.0 이 그것을 밟았다(`.private-names` 차단이 소스에만
있고 어느 저장소에도 설치되지 않은 채 "활성"으로 보고). v3.8.0 이 원인(install-once)을
고쳤지만, **고친 것과 다음번에 같은 형태가 안 지나가는 것은 다른 문제다.**

**경고이지 오류가 아니다** — §3b·§19 와 같은 비대칭. 훅 소스를 고치는 중에는 repo 가
설치본보다 앞선 것이 정상이고, red 로 두면 훅 작업 내내 발화해 죽은 경고가 된다
(`warning-signal.md` §검토 절차 1).

검사 조건 한 문장: **설치된 pre-commit 이 존재하고 킷 마커를 갖고 있는데 repo 정본과
내용이 다르면 노란 줄.** 마커가 없으면 소비자 소유이므로 대조를 생략한다.

세 상태를 전부 실측했다 `[confirmed]` — 초록 **도달 가능**(session-check 가 갱신한 뒤),
드리프트 시 노랑(되돌려-FAIL), 사용자 훅이면 침묵. 초록이 도달 불가능한 경고는 그 자체로
죽은 경고이므로 이 확인이 검사의 일부다.

로컬 전용이다. CI 에는 `~/.claude` 도 설치된 훅도 없다 — 없는 것을 검사한다고 주장하지
않는다.


## [3.10.0] — 2026-09-09

**진입점이 하나가 아니다.** 규범은 하네스 중립인데 내보내는 파일은 `AGENTS.md` 하나뿐이라,
`GEMINI.md` 를 읽는 하네스는 규율 밖에서 돌고 있었다.

### Added — 다중 진입점 export (`AGENTS.md` · `GEMINI.md`)

하네스마다 **읽는 파일 이름이 다르다**: Codex·OpenCode·Copilot·Cursor 는 `AGENTS.md`,
Gemini CLI 계열은 `GEMINI.md`. **내용은 같다** — 블록도 sha 도 하나이고 파일만 여럿이다.

- `ENTRYPOINTS` 상수가 목록의 SSOT. `--entrypoints a.md,b.md` 로 덮어쓴다
- 전문(preamble)의 H1 에서 파일 이름을 뺐다(`# Agent instructions`) — 넣으면 파일마다
  전문이 갈리고, 전문이 sha 에 들어가므로 **진입점마다 sha 가 달라진다.**
  하나의 블록·하나의 sha·여러 파일이 이 설계의 요점이다
- `--check` 는 **첫 실패에서 멈추지 않는다.** 멈추면 "하나만 낡음"과 "전부 낡음"을
  구별할 수 없어 사람이 재실행을 두 번 하게 된다
- §11 게이트가 그대로 전 진입점을 덮는다 — **부재도 red 로 잡는다**(실측 확인).
  새 게이트를 만들지 않았다

**목록은 정책 파일이 아니라 모듈 상수다.** 소비자의 플러그인 캐시에는 `packaging/` 이
없다 — 훅은 자기가 설치된 곳에서 자족해야 한다(consumer-first).

**`CLAUDE.md` 는 의도적으로 뺐다.** Claude Code 는 SessionStart 훅이 규범을 직접
주입하므로 파일로 또 실으면 같은 규범이 두 번 들어간다. 훅이 없는 하네스만 파일이 필요하다.

### 회귀 테스트 3종

전 진입점 동일 블록 · `--check` 가 전부를 보고 · `--entrypoints` 덮어쓰기.

### 알려진 한계 (탐지하지 않는 것은 탐지한다고 적지 않는다)

§15 크기 예산은 **`AGENTS.md` 전용으로 남긴다.** 그 상한의 근거는 Codex 의
`project_doc_max_bytes`(32 KiB 병합 총량, 초과 시 조용히 잘림)라는 **실측된 값**이다.
Gemini 계열의 대응 상한은 실측하지 않았으므로 숫자를 지어내지 않는다 —
`docs/conventions/warning-signal.md` 의 "탐지할 수 없는 것은 탐지할 수 없다고 적는다".


## [3.9.0] — 2026-09-09

마지막 개명 잔재였던 **런타임 경로와 마커 토큰**을 제품명과 무관한 이름으로 옮겼다.
§20 게이트에 소문자 경로 이름을 추가하자 드러난 것이고, 게이트가 제 일을 한 사례다.

### Changed — 자식 마커·레지스트리 경로: `<gitdir>/cck/` → `<gitdir>/kit/`

**제품명을 경로에 박은 것이 애초 결함이다.** 이 경로는 내부 경로가 아니라 **프로토콜
경로**다 — `rules/child-marker.md`(규범)·`skills/child-session`(쓰는 쪽)·
`hooks/examples/child-git-guard.py`(읽는 쪽)·`feedback_ledger.py`(레지스트리 포인터)·
`scripts/work.sh`(W-ID claim 레지스트리)가 같은 이름을 참조한다. 제품명이 들어가 있으면
**개명할 때마다 살아있는 마커가 고아가 된다.** 그래서 다음 개명에 깨지지 않는 중립
이름을 골랐다.

이 저장소에 살아있는 마커는 **0건**이었고(`find .git -name cck` → 없음), 외부 소비자도
없음을 확인한 뒤 옮겼다 `[confirmed]`.

### Changed — AGENTS.md 마커 토큰: `cck:`/`cck2:` → `kit:`/`kit2:` (**이행 경로 포함**)

소비자의 `AGENTS.md` 에는 이미 구 토큰 블록이 들어 있다. 새 토큰만 인식하면 그 블록이
**고아로 남은 채** 새 블록이 덧붙어 한 파일에 규범 블록이 둘이 된다. 그래서:

- **읽기**는 구·신 토큰을 **둘 다** 인식한다 (`(?:cck|kit):`)
- **쓰기**는 새 토큰으로만 내보낸다
- 결과적으로 다음 export 가 **제자리에서 교체**한다 — 회귀 테스트로 고정했다
  (블록 1개 · 구 토큰 0건 · 마커 밖 사용자 콘텐츠 불가침)
- 자기 손상 가드도 구·신 토큰을 둘 다 본다 — 구 토큰이 룰 본문에 들어와도 같은 사고다

그 전까지 `--check` 는 드리프트로 red 를 내는데 **그것이 옳은 신호다**(재생성하라는 뜻).

### Added — §20 에 줄 단위 예외 `old-name-ok`

구 토큰을 **정당하게** 담는 줄이 생겼다(이행 경로). 파일 단위 제외로 처리하면 그 파일의
**미래 잔재까지 영영 안 잡힌다** — `warning-signal.md` §검토 절차 5 가 경고하는 형태
그대로다. 그래서 그 **줄에만** `old-name-ok` 를 적고 나머지는 계속 검사한다. 파일 단위
제외는 역사 기록(`docs/`·CHANGELOG)에만 쓴다.

되돌려-FAIL 확인: 같은 파일에 표기 없는 새 잔재를 넣으니 줄 번호와 함께 잡혔다.

### Fixed — 약칭·경로 이름까지 §20 범위에 (`previousNames`)

`previousNames` 가 이제 정식 이름·약칭·경로 이름 셋을 담는다. 이번에 드러난 것:

- `\bCCK\b` 정규식은 **한글 조사가 붙은 `CCK의` 를 놓친다** — 그 형태로 4건이 더 있었다.
  §20 이 리터럴 부분문자열 방식인 것이 단어 경계 방식보다 강하다는 실증이다
- `scripts/work.sh` 의 상태 디렉토리 변수가 `CCK_STATE_DIR` 였다 → `KIT_STATE_DIR`
- `scripts/check_registry_describe.py` 의 산문 11곳이 제품을 구 이름으로 지칭했다


### 개명 잔재 — 약칭 'CCK' 10건, 그중 4건은 단어 경계 검사로는 안 잡혔다 (2026-09-09)

`plugins/` 변경(산문 주석·독스트링만, 동작 무변경). 방금 넣은 §검토 절차 5("인스턴스를
고칠 때 클래스를 묻는다")를 §20 자신에게 적용한 결과다 — 개명 잔재는 정식 이름만이
아니라 **약칭**으로도 남는다.

`previousNames` 에 `CCK` 를 추가했다. 실측으로 드러난 것:

- `\bCCK\b` 정규식은 **한글 조사가 붙은 `CCK의` 를 놓친다** — 그 형태로 4건이 더 있었다.
  §20 이 리터럴 부분문자열 방식인 것이 단어 경계 방식보다 강하다는 실증이다.
- `scripts/work.sh` 의 **상태 디렉토리 변수**가 `CCK_STATE_DIR`/`cck_state_dir` 였다 —
  내부 식별자이므로 `KIT_STATE_DIR`/`kit_state_dir` 로 바꿨다.

**온디스크 경로 `$(git-common-dir)/cck` 는 바꾸지 않았다.** 이것은 내부 경로가 아니라
**프로토콜 경로**다: `rules/child-marker.md`·`skills/child-session`·
`hooks/examples/child-git-guard.py`·`feedback_ledger.py` 의 레지스트리 포인터가 같은
이름을 규범으로 참조하고, 외부 오케스트레이터의 registry `describe` 이음매도 이 경로를
읽는다. 옮기려면 소비자와 함께 옮겨야 하므로 **조율 안건으로 남긴다** — 살아있는 마커를
고아로 만들지 않는다. `previousNames` 가 대소문자를 구분하므로 소문자 `cck` 는 검사에
걸리지 않고, 그것이 의도다(사유는 `packaging/name-targets.json` 에 기록).

### `claude plugin details` 가 중첩 에이전트를 세지 않는다 — 원인 확정 (2026-09-09)

`plugins/` 무변경. 열려 있던 `[unresolved]` 를 닫았다.

**원인**: `plugin details` 는 `agents/*.md` 만 세고 **하위 디렉토리를 무시한다.**
스크래치 플러그인에 `agents/flat-one.md` 와 `agents/sub/nested-one.md` 를 동시에 두고
실행하니 `Agents (1) flat-one` 만 나왔다 `[confirmed]`. 대조군도 일치한다 —
`code-simplifier` 는 `agents/code-simplifier.md` 로 **평평**해서 `Agents (1)` 을 낸다.
이 킷의 33종은 전부 `agents/{category}/` 라 통째로 안 보인다.

**영향**: `Always-on ~1,386 tok` 추정에 **에이전트 description 이 빠져 있다** —
실측 7,844B ≈ 1,961 tok(frontmatter 전체는 14,092B ≈ 3,523 tok). §16 이 재는 축
(core 규범 + WORKFLOW, 9,986B/10,240B) **밖에 그만한 상시 비용이 또 있다.**

**평탄화하지 않는다.** 에이전트 호출 id 에 카테고리가 들어가므로(`<플러그인>:dev:fix-bugs`)
평탄화는 33종의 id 를 바꾸는 파괴적 변경이고, 얻는 것은 CLI 리포트 정확도뿐이다.
도구가 못 세는 것을 우리가 대신 세는 쪽이 맞다 — §16 예산 재설계에서 이 축을 다룬다.

### 경고·검사 검토 절차에 두 질문 추가 (2026-09-09, 레포 로컬)

`plugins/` 무변경. `docs/conventions/warning-signal.md` 에 §검토 절차 5·6 을 넣었다.
v3.8.0 에서 잡은 두 결함이 **같은 뿌리**였다 — 만든 사람이 자기가 만든 것의 경계를
묻지 않았다.

- **5. 이 검사의 대상 밖은 어디인가.** 나열-대상 검사는 목록 밖을 결코 red 로 만들지
  못한다. 인스턴스를 고칠 때 클래스를 묻고, 가능하면 **대상이 아니라 제외를 나열**한다.
  v3.3.0 이 `site/hugo.toml` 하나만 목록에 추가하고 끝낸 결과 `setup.sh` 가 전임
  플러그인을 설치하는 상태가 v3.0.0~v3.7.0 내내 유지됐다.
- **6. 이 출력은 어디까지 가는가.** 검사의 실패 메시지가 보호 대상 자체를 인쇄하면
  그 검사의 전제가 무너진다. 훅 출력은 트랜스크립트·CI 로그로 이동한다.

두 질문 다 §검토 절차 4("이 검사가 도는 조건을 한 문장으로 쓸 수 있는가")의 이웃이다 —
4는 *언제 도는가*, 5는 *무엇을 보는가*, 6은 *무엇을 뱉는가*.


## [3.8.0] — 2026-09-09

전임 킷에서 넘어온 **개명 잔재를 살아있는 표면에서 전부 걷어내고**, 그것이 다시 실리지
않게 게이트를 걸었다. 그 과정에서 훅 설치가 영구 동결되는 결함을 찾아 고쳤다.

### Fixed — 훅이 한 번 설치되면 영원히 갱신되지 않았다 (`setup/session-check.py`)

`not hook_dst.exists()` 조건이라 **훅이 없을 때만** 설치했다. 그래서 최초 설치 시점 판이
영구 동결되고, 이후 릴리스에서 추가된 검사가 기존 사용자에게 **영원히 도달하지 않았다.**

실측으로 드러난 경로: v3.7.0 이 *"비공개 이름 가드 활성화"* 로 보고했으나, 그 검사는
`setup/pre-commit` 소스에만 있었고 **어느 저장소에도 설치되지 않았다**(`private-names`
문자열 출현 — 소스 8회 / 설치본 0회, 두 클론 모두). 배포된 것과 실행되는 것이 갈리는
이 킷의 대표 결함 클래스가 설치 시점에서 재현된 것이다.

**수정**: 킷이 심은 훅(마커 `# Auto-installed by session-check.py` 보유)만 내용이 다를 때
갱신한다. 마커가 없으면 **사용자 소유 훅**이므로 손대지 않고, 읽을 수 없으면 판정 불가로
보고 역시 손대지 않는다. 갱신하면 stderr 로 알린다 — 사용자 저장소의 실행 파일이 바뀐
사건을 조용히 처리하지 않는다. 회귀 테스트 4종(설치·갱신·사용자 훅 불가침·최신이면
무동작)을 붙였고, 구 동작으로 되돌려 갱신 테스트만 실패하는 것을 확인했다.

### Fixed — 개명 잔재 3건이 **기능 결함**이었다

| 위치 | 증상 |
| --- | --- |
| `setup.sh` | `claude plugin install <구 이름>@<구 이름>` — **설치 스크립트가 전임 플러그인을 설치했다** |
| `skills/harness-export` | 캐시 fallback glob 이 구 이름 디렉토리를 찾아 영영 매치되지 않음 |
| `skills/plan-task` (checklist 폴백) | 같은 형태 |

그 밖에 사용자 가시 문자열도 구 이름이었다: `session-check` 의 stderr 프리픽스 3곳(이제
매니페스트에서 파생 — D-3), 스킬 설명·헤더, 사이트 브랜드·푸터·URL, 테스트 픽스처 경로.

### Added — §20 게이트: 살아있는 표면에 구 이름 0건 (`scripts/check_old_names.py`)

§18(이름 SSOT 파생)은 `files`/`directories` 에 나열된 **산문 문서만** 본다. `plugins/`·
`site/`·`setup.sh` 는 대상이 아니었고, 그 구멍으로 위 결함이 전부 통과했다 — "검사 대상이
아닌 것은 결코 red 가 되지 않는다"(D-23)의 실측 사례다.

검사 조건 한 문장: **`oldNameScanExclude` 에 없는 git-tracked 파일에 `previousNames`
문자열이 하나라도 있으면 fail.** 분기가 없어 항상 돈다. 제외는 구 이름이 사실로서
등장해야 하는 곳뿐이다 — 역사 기록(`docs/`·`CHANGELOG.md`)과 정책 파일 자신.
구 이름은 매직 리터럴이 아니라 정책에서 읽는다(F-022). `verify-done.sh §20` 과 CI 가
**같은 스크립트**를 호출한다(F-023). 되돌려-FAIL 확인 완료.

### Security — 가드의 실패 메시지가 자기가 막던 이름을 그대로 찍었다 (`setup/pre-commit`)

`.private-names` 검사가 위반을 알릴 때 **매치된 이름을 원문 그대로** 출력했다. 로컬
터미널만 생각하면 친절한 메시지지만, 훅 출력은 거기 머물지 않는다 — 에이전트
트랜스크립트·CI 로그·이슈 첨부로 옮겨 다닌다. **이 배치의 되돌려-FAIL 검증 중에 실제로
대화 로그에 실렸다.** 목록을 커밋하지 않는다는 이 설계의 전제가 출력 한 줄로 무너진다.

**수정**: 첫 글자와 길이만 남긴다(`h…(5자) → doc.md`). 소유자는 자기 목록을 알고 있으므로
어느 항목인지 식별에는 충분하고, 로그로 새어도 이름이 복원되지 않는다. 차단 동작은 그대로다.

### Added — 위임 계약: 사실에는 등급을 붙여 보낸다 (`rules/delegation-contract.md`)

보고·인계에 실린 사실은 등급(`[confirmed]` / 소스 기준 / 미확인)과 함께 보내고, 등급 없는
단정 중 **자기 행동을 바꾸는 것**은 받는 쪽이 확인한다.

실측 근거가 바로 위 훅 결함이다. 한 세션이 가드를 임시 레포에 손으로 복사해 검증하고
*"활성"* 으로 보고했고(= 소스 기준을 confirmed 로 승격), 받은 세션이 실물 확인 없이
접수했다. 격리 픽스처 통과가 세션 경계를 넘으며 조용히 등급이 올라가는 형태다 — 등급을
같이 보냈으면 받는 쪽이 그 자리에서 한 줄 grep 으로 끝냈을 일이다.

### Changed — git 히스토리를 단일 커밋으로 재시작

전임 킷과 공유하던 300 커밋·46 태그를 정리하고 v3.8.0 트리 하나로 다시 시작했다.
이로써 히스토리에만 남아 있던 **비공개 프로젝트 이름 6 커밋**도 함께 사라진다(현재
트리·배포 표면은 이미 0건이었다). 카탈로그 pin 이 걸린 항목이 없어(미등재) 외부 파급이
없는 시점이었다.

**지식은 파일에 있다.** 과거 릴리스 기록은 `docs/CHANGELOG-archive.md` 로 옮겼고, 설계
근거는 `docs/specs/`·`docs/conventions/` 에 그대로 있다. 이 킷이 커밋 메시지가 아니라
추적되는 파일에 근거를 적어 온 관례 덕분에 재시작 비용이 낮았다.

### Changed — README·사이트에서 전임 킷 안내 제거

마이그레이션 절을 걷어내되, **킷과 무관한 운영 사실**은 남겼다 — 플러그인은 user scope 라
설치·갱신·제거가 그 머신의 **모든 세션**에 영향을 주고, 이름이 다른 두 플러그인이 같은
스킬을 실으면 dedup 키가 `<플러그인 이름>:<스킬 이름>` 이라 **경고 없이 두 번 등록된다**
(실측: `docs/specs/2026-09-07-rename-probe.md`).

### Security — `.private-names` 가드가 **설치된 적이 없다** (2026-09-09, plugins/ 무변경)

`plugins/` 무변경. 문서만. v3.7.0 이 *"비공개 이름 가드 활성화"* 라고 보고했으나 **거짓
이었다** — 검사는 소스에만 있고 어느 저장소에서도 동작하지 않는다.

**실측** (`private-names` 문자열 출현 횟수):

| 위치 | 크기 | 출현 |
| --- | --- | --- |
| `plugins/common/setup/pre-commit` (소스) | 6,263B | **8** |
| 이 레포 클론 `.git/hooks/pre-commit` | 2,222B | **0** |
| 전임 레포 클론 `.git/hooks/pre-commit` | 2,864B | **0** |
| 플러그인 캐시 `setup/pre-commit` | 2,864B | **0** |

설치된 훅이 실제로 하는 일은 ruff check 하나다. 해당 문자열이 0회 등장하는 스크립트는
그 목록을 읽을 수 없다 — 되돌려-FAIL 없이 정적으로 결론난다.

**원인**: `setup/session-check.py` 가 `not hook_dst.is_symlink() and not hook_dst.exists()`
조건에서만 훅을 설치한다. **없을 때만 설치하고 갱신 경로가 아예 없다.** 훅은 최초 설치
시점 판에서 동결되고, 설치된 플러그인 자체도 옛 버전이면 새 소스는 애초에 도달하지 않는다.
결과적으로 이 가드는 **만들어진 이래 한 번도 활성이었던 적이 없다.**

**소비자 파급이 본체다.** 킷을 일찍 설치한 사용자는 pre-commit 이 그 시점 판에 영구
동결된다 — 훅을 고쳐도 기존 사용자에게 가지 않는다. CLAUDE.md 의 north-star("설치한
사용자 환경에서 동작하는가")에 정면으로 걸리고, 이 레포가 이미 아는 결함 클래스
*"편집이 현재 세션에 반영되지 않는다"* 의 **설치 시점 변종**이다.

**이번에 한 것은 문서 정정뿐이다.** `README.md` 에 조건부 서술 + 확인 명령
(`grep -c private-names "$(git rev-parse --git-path hooks)/pre-commit"`) + 갱신 안 함이라는
사실을 적었다. 갱신 안 함은 **코드 밖 어디에도 문서화돼 있지 않았다** —
`warning-signal.md` 의 *"탐지할 수 없는 것은 탐지할 수 없다고 적는다"* 와 같은 계열이라
정책 결정과 분리해 먼저 적는다.

**미결(정책 결정 필요)**: `session-check.py` 의 install-once 를 어떻게 할 것인가 —
①버전·해시 대조 후 갱신(소비자가 손댄 훅도 덮음) ②킷이 심은 마커 줄이 있을 때만 갱신
(손댄 훅 존중) ③갱신하지 않고 낡았을 때 경고만. 사용자 파일을 덮는 행위라 결정 전까지
구현하지 않는다.

**함께 내려간 것**: *"비공개 이름 재유입이 가드로 막힌다"* 는 주장은 근거를 잃었다.
현재 트리·배포 표면 0건은 유지되지만 그 이유는 기계 보장이 아니라 **상태 관찰**이다.
히스토리 재작성이 급하지 않다는 결론 자체는 유지되나 근거 등급이 내려간다.


### Security — 비공개 프로젝트 이름이 현재 트리에 있었다 (2026-09-08, plugins/ 무변경)

`plugins/` 무변경. 문서·스캐너 설정만. 전임 킷과 히스토리를 공유하므로 **같은 결함이
양쪽에 있었다** — 양쪽 다 고쳤다.

**발견**: 이번 달 설계 문서 3곳의 권한 예시가 `Bash(<실제 비공개 프로젝트명>:*)` 형태였다.
중립 자리표시자로 치환 — 현재 트리 0건.

**gitleaks 가 이것을 놓쳤다.** 룰 정규식이 `_(quant|optimizer|ops)` **접미사만** 봐서,
접미사 없이 쓰인 이름은 하나도 걸리지 않는다. 히스토리 전수로 정밀도도 실측했다 —
매치 24건 중 참 양성 **1건(4%)**, 나머지는 `total_*`·`git_*`·`write_*` 꼴의 일상
식별자. **잡아야 할 것은 놓치고, 안 잡아도 될 것만 잡는** 두 방향 실패였다.

**조치**: `_ops` 제거로 오탐 제거(37 → 23건, 현재 트리 0건). 그리고 **패턴으로는 임의의
비공개 이름을 탐지할 수 없다**는 한계를 `.gitleaks.toml` 주석에 명시했다 — 이름을 알아야
잡히고, 그 이름을 공개 설정 파일에 적으면 그것이 곧 유출이다. 없는 보호를 있다고 믿게
하지 않는다(`docs/conventions/warning-signal.md`). 기계적 방어가 필요하면 이름 목록을
**gitignore 된 로컬 파일**에 두고 `setup/pre-commit` 이 읽는 형태가 맞다 — 미구현.

> **히스토리는 고치지 않았다.** 재작성은 모든 SHA 를 바꾸므로 되돌릴 수 없고 외부에
> 영향이 있다(전임 레포의 경우 카탈로그 pin 무효화). 저장소 소유자 판단 영역이다.

### 카탈로그 실태 전수 조사 + 등재 절차 변경 반영 (2026-09-08, 레포 로컬)

`plugins/` 무변경. 조사 결과를 문서에 근거와 함께 박고, 실제로 바뀐 등재 절차를 반영했다.

**신설**: `docs/research/2026-09-08-plugin-directory-status.md` — 사실 3분류 태그를 붙인
전수 조사와 **재현 명령**. 결론만 남기면 다음 사람이 근거를 다시 만들어야 한다.

**정지 범위를 실측했다** `[researched: GitHub API, n=60 무작위 표본]`. 직전 릴리스는
*"모든 항목이 똑같이 멈춰 있다"* 를 **추론**으로 적었다 — 표본으로 확인했다:

- 업스트림이 2026-08-14 이후 움직인 항목 **11건 중 bump 0건**
  (`tavily` 3개월·`email-assistant` 5개월 뒤처짐)
- 신규 등재도 정지 — 파이프라인 일괄 등재는 **8/07**, 마지막 등재는 8/21(기여자 PR 수동 머지)
- `Add referodesign` PR(#2355)이 **8/11부터 열린 채**, 레포 전체 커밋 8/24 이후 0건
- **표본의 "핀=HEAD 44건" 은 정상 작동의 증거가 아니다** — 업스트림 자체가 휴면인
  항목들이라 따라갈 것이 없어서 최신인 것이다. 이 구분을 하지 않으면 표본이 정반대로 읽힌다

**자매 카탈로그 대조로 범위가 좁혀졌다**: `claude-plugins-official`·
`knowledge-work-plugins` 는 2026-09-04 에도 bump 를 머지했다. **조직 전체 중단이 아니라
community 카탈로그만** 멈춘 것이다. 왜인지는 `[unresolved]` 로 남긴다.

### Changed — 제출 폼이 둘로 갈렸다 `[researched: code.claude.com/docs/en/plugins]`

| 경로 | 자격 |
| --- | --- |
| `platform.claude.com/plugins/submit` (Console) | **조직에 속하지 않은 개인 저자** ← 우리 경우 |
| `claude.ai/admin-settings/directory/submissions/plugins/new` | Team/Enterprise 조직 + 디렉토리 관리 권한 |

우리 문서는 Console 만 알고 있었다. 개인 저자에게는 그게 맞아 **결과적으로 틀리진
않았지만**, 자격 조건이 없어 Team/Enterprise 사용자가 이 문서를 따르면 잘못된 경로를 쓴다.

### Confirmed — §19 게이트는 추론이 아니라 문서화된 요구사항이었다

공식 문서: *"Run `claude plugin validate ./your-plugin` before you submit. **The review
pipeline runs the same check on every submission.** Warnings don't fail validation; add
`--strict` to treat them as errors."*

v3.6.0 에서 추론으로 세운 근거가 문서에 명시돼 있었다. 경고가 실패가 아니라는 점도
확인됐으므로 **`--strict` 는 상류보다 엄격한 우리 선택**이지 요구사항이 아니다 —
그대로 유지한다.

### 부수 발견 — `claude plugin details` 의 always-on 추정이 에이전트를 세지 않는다

`claude plugin details` 가 우리 플러그인을 **`Agents (0)`** 으로 보고한다(실제 33종).
로드는 정상이다(`kit:dev:explore-codebase` 로 호출 가능, `validate agents/` 통과) —
**집계만 0** 이다. 따라서 보고된 `Always-on: ~1,518 tok` 에 에이전트 설명이 빠져 있다:
33종 `description` 합계 **7,745B ≈ 1,936 tok** 으로, 보고된 always-on 전체보다 크다.

**함의**: §16 주입 예산(10,240B)은 core 규범 + WORKFLOW 만 센다. **비슷한 크기의 축이
예산 밖에 있다.** 중첩 레이아웃이 집계 0 의 원인인지는 `[unresolved]` — 평평한 레이아웃
대조 실험을 아직 하지 않았다. 예산 재설계 시 다룬다.

### 카탈로그 전파 서술을 실측으로 정정 (2026-09-08, 레포 로컬)

`CLAUDE.md` 와 `docs/marketplace-submission.md` 가 *"pin 은 자동 전진하고 nightly
동기화라 하루면 전파된다"* 고 단언하고 있었다. 2026-07 관측이고 **지금 실측과 어긋난다.**

전임 킷 `claude-code-kit` 항목 기준 실측: 마지막 bump 가 **2026-08-09**(v2.12.3)이고
그 뒤 2.13.0~2.20.0 이 반영되지 않았다(164 커밋). `marketplace.json` 커밋 300개 중 그
항목 bump 는 **1건**뿐이며, 카탈로그 미러 레포는 **2026-08-24 이후 커밋이 없다.**
그런데 카탈로그 README 는 여전히 *"synced nightly"* 라고 적혀 있다.

8/9~8/24 구간엔 다른 항목은 bump 되고 있었으므로 **그 항목만 누락된 구간**이 있고,
8/24 이후로는 미러 전체가 멈췄다. **원인은 레포 밖에서 판정할 수 없다.**

### Fixed — `CLAUDE.md` 설치 블록이 없는 경로를 안내하고 있었다

`hiway-kit` 은 카탈로그 **미등재**인데 설치 예시 첫 줄이
`/plugin install hiway-kit@claude-community` 였다. 제거하고 미등재 사실을 명시했다.
v3.3.0 에서 README 의 같은 결함을 고쳤는데 `CLAUDE.md` 는 놓쳤다 — **같은 주장이 여러
표면에 복제돼 있으면 한 곳만 고치고 끝났다고 믿게 된다.**

### 부수 — 이름 파생 게이트가 자기 역할을 했다

이번 편집으로 `CLAUDE.md` 가 전임 킷 이름을 **정당하게** 부르게 되자 §18 이 즉시 red 를
냈다. `--write` 를 돌렸다면 그 이름이 치환돼 **실측의 대상이 바뀌고 서술이 거짓이 됐을
것이다.** `previousNameAllowlist` 에 사유와 함께 등재했다(2/16 대상).

v3.3.0 에서 허용목록을 만들 때의 판단 — *"마이그레이션 안내는 구 이름을 불러야 한다"* —
이 예상하지 않은 두 번째 사례로 확인됐다. 다만 **허용된 파일 안의 진짜 잔재는 여전히
탐지되지 않는다**는 한계도 그대로다.

### 검사가 도는 조건을 한 문장으로 쓴다 (2026-09-08, 레포 로컬)

`plugins/` 무변경이라 소비자 동작이 바뀌지 않는다. 캐시 키를 움직일 이유가 없다.

**규율 신설** — `docs/conventions/warning-signal.md` §검토 절차에 4번째 항목.

> **이 검사가 도는 조건을 한 문장으로 쓸 수 있는가. 쓸 수 없으면 안 도는 것이다.**

상시 참인 경고(1~3번)의 **거울상**이다. 그건 항상 발화해서 죽고, 이건 한 번도 발화하지
않아서 죽는다. 그리고 이쪽이 더 안 보인다 — 코드도 호출부도 있고 커버리지도 초록이다
(그 코드는 *다른* 조건에서 실제로 실행되니까). 없는 것은 grep 이 찾아 주지만 **도달
경로만 없는 것**은 찾아 주지 않는다.

실측 `[confirmed: n=2]`: 이 레포의 `derive-name.py` 잔존 검사(v3.3.0 에서 수정) ·
외부 세션의 알림 함수(예외를 삼키고 `bool` 반환 → 예외 경로 도달 불가).

**규율을 우리 게이트에 즉시 적용했고, 두 건이 나왔다.**

### Fixed — CI 가 이름 파생 게이트(§18)를 돌린 적이 없다

CI 에는 드리프트 게이트가 넷 있어야 하는데 셋뿐이었다 — AGENTS.md(§11)·타겟
매니페스트(§14)·문서 카운트(§6)·eval 커버리지(§13) 는 있고 **이름 파생(§18)만 없었다.**
발화 조건이 *"누가 로컬에서 `verify-done.sh` 를 돌릴 때만"* 이었다는 뜻이다 — 개명 PR 이
파생을 안 돌리고도 CI green 으로 통과할 수 있었다. 스텝을 추가했다(로컬과 동일 스크립트).

### Added — `check_registry_describe.py` 가 게이트에 물려 있지 않다는 사실을 본문에 명시

전수 대조 결과 이 프로브는 `verify-done.sh` 도 CI 도 부르지 않는다. **의도된 것이다** —
발화 조건이 *"컨트롤 프로젝트가 실제로 `describe` 를 내놓았을 때 사람이 지목해 부른다"*
인데 그 실물이 아직 없다(설계 SSOT §20.2). 대조할 것이 없는데 게이트에 물리면 "검사
대상 0개"를 green 으로 보고하게 되고, **그건 정합이 아니라 손상이다.**

문제는 그 사실이 **어디에도 적혀 있지 않았다**는 것이다 — 스크립트만 보면 게이트에
물려 있다고 오해한다. 독스트링에 발화 조건과 그 근거를 적었다. 없는 보호를 있다고
믿게 하지 않는다.

> 같은 대조에서 `work.sh`·`bump-version.sh`·`feedback.sh`·`checklist.sh`·`eval-forge.py`
> 도 게이트 밖이지만 **이들은 사람·스킬이 부르는 CLI 이지 검사가 아니다** — 결함이 아니다.



W-024 배치. **버전을 올리지 않는다** — 변경이 `evals/`·`scripts/`·`docs/` 로 전부 레포
로컬이고 `plugins/` 아래 배포물은 무변경이라 소비자 동작 변경이 없다. 플러그인 캐시 키를
움직일 이유가 없다(스펙 D-4).

### Fixed — eval 게이트가 자기 정책이 강제한다고 주장한 검사를 하고 있지 않았다

`evals/policy.json` 의 `_tier2Rationale` 은 *"tier2 는 경고 수준 커버리지로만 다룬다"* 고
적어 뒀지만, `scripts/check_eval_coverage.py` 에는 **문자열 `tier2` 가 한 번도 나오지
않았다** — 경고조차 구현돼 있지 않았다.

그 결과 `consensus-builder` 가 `tiers.tier2`(A등급)에 등재된 채 **시나리오 없이 조용히
통과**하고 있었다. `git log --all -- 'evals/scenarios/consensus-builder*'` 결과 0건 —
어느 커밋에도 존재한 적이 없다. 같은 파일의 회계 문장(*"티어1 13 + 티어2 A 16 + B 3 +
C 1 = 33종, 미분류 잔여 0"*)은 **거짓이었다**(실제 시나리오 28종).

게이트가 놓친 기전은 단순하다: 기존 검사 셋(시나리오⊆기준선, 기준선⊆시나리오, **티어1**
최소 보유) 중 어디에도 걸리지 않는다. 시나리오가 없으니 앞의 두 집합에 등장하지 않고,
티어1이 아니니 세 번째도 지나간다. **검사 대상이 아닌 것은 결코 red 가 되지 않는다.**

### Added — 커버리지 게이트에 검사 2종

| 검사 | 대상 | 판정 | 플래그 |
| --- | --- | --- | --- |
| `check_tier2` | `tiers.tier2`(A등급) 각 에이전트 최소 시나리오 | **fail** | `gate.tier2CoverageEnforceFail: true` |
| `check_classification_complete` | 전체 에이전트 = tier1 ∪ tier2 ∪ `_tier2Classification`(B·C) | **warn** | `gate.classificationCompleteEnforceFail: false` |

- 티어2를 **도입 즉시 fail** 로 놓은 이유: 이 배치가 유일한 갭을 같은 배치에서 메우므로
  승격 직후 16/16 green 이다. `tier1CoverageEnforceFail` 이 S1 경고 → S4 fail 로 간 것과
  달리 단계를 나눌 이유가 없었다. 영구 노란 경고는 아무도 보지 않는다.
- 분류 완전성을 **warn** 으로 둔 이유: 새 에이전트를 추가하는 무관한 작업이 분류 등재
  전까지 red 가 되면 게이트가 작업을 막는 방향으로 오작동한다. 경고는 매 실행 출력된다.
- **"검사 대상 0개"는 정합이 아니라 손상이다.** `tiers.tier2` 부재·빈 배열, 그리고
  `plugins/common/agents` 에서 에이전트 0종 발견은 **플래그와 무관하게 즉시 fail** 이다.
  후자는 이 배치의 1차 구현에 실제로 있던 결함이다 — 통합 전 리뷰에서 `plugins/` 없는
  가짜 레포로 실증했다(`✓ 전체 에이전트(0종) 분류 완전` + exit 0). `validate_policy_schema`
  가 이미 기록한 거짓 green 이 새 검사에 재도입된 것이라, 같은 배치 안에서 봉쇄했다.

### Added — `consensus-builder` 시나리오 `hard-constraint-standoff`

티어2 A등급 중 유일하게 비어 있던 자리를 메운다. 잡는 실패는 **"위장된 합의"** — 양보 불가
제약을 깎아 만든 가짜 Win-Win 을 합의로 보고하는 것으로, W-023 에서 `git-workflow` 가
충돌을 `--theirs` 로 밀고 *"성공적으로 병합되었습니다"* 라고 한 것과 같은 클래스다.

fixture 는 Round 1 종합 결과에 충돌 2건을 둔다 — **#1 Hard vs Hard**(GDPR EU 역내 저장
의무 vs SLA 조항 7.2 "서로 다른 대륙 2개 리전 복제"), **#2 협상 가능**(4주 출시 vs 6주
정합성 검증). 올바른 행동은 #1 을 사용자 결정으로 올리고 #2 에만 합의안을 내는 것이다.

초안 fixture 에는 **EU 다중 리전 복제라는 기술적 우회로가 실제로 존재**해 에이전트가
정당하게 조건부 합의를 냈다(어서션은 통과했으나 의도한 hard-constraint 강제가 성립하지
않았다). SLA 원문에 *"동일 대륙 내 복제는 본 조항을 충족하지 않는다"* 를 명문화해 닫았다 —
**어서션이 아니라 fixture 를 고쳤다.** 실측 연속 2회 통과로 검증했다.

에스컬레이션 어서션은 통합 전 리뷰에서 한 번 좁혔다: 값에 있던 `"사용자에게"` 는
*"사용자에게 보고드립니다"* 같은 평범한 서술에도 매치돼 **이 어서션이 잡으려던 실패가
그대로 통과**한다. 빼는 과정에서 실측 출력이 `❓ 사용자(경영진) 결정 필요` 였고 중간의
`(경영진)` 때문에 `"사용자 결정"` 과 매치되지 않는다는 것 — 즉 목록이 사실상 이모지
`❓` 하나에 의존하고 있었다는 것 — 이 드러나, `"결정 필요"`·`"에스컬레이션"` 을 넣었다.
**약화가 아니라 강화다.**

### Changed — 기준선 재생성 (`2026-08-31.json` → `2026-09-04.json`)

시나리오 추가로 `requireBaselineCoversAllScenarios` 가 red 를 냈고,
`baseline._meta.regenerationCadence` 규정대로 같은 배치에서 전량 재실행했다(부분 실행은
`--baseline` 저장이 거부된다). **38/38 pass, 기존 37건 status 전부 유지(회귀 0).**
