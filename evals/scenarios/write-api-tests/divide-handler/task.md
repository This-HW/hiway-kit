# 과제

`endpoints.py`의 `divide_handler`에 대한 API 테스트를 작성하라(pytest).

요구사항:
- 테스트 파일은 같은 디렉토리에 `test_endpoints.py`로 작성한다.
- 최소 다음 케이스를 포함한다: 정상 나눗셈(200), `a`/`b` 누락(400),
  0으로 나누기(400).
- `endpoints.py`(구현 코드) 자체는 수정하지 않는다.
