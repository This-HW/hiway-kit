# 과제

`dupes.py`의 `find_duplicates` 함수는 O(n²) 시간복잡도로 동작한다. 이를 O(n)으로
최적화하라(해시 기반 — `set` 사용을 권장).

요구사항:
- 반환값의 의미(중복 값들을 중복 없이 반환, 순서는 무관)는 그대로 유지한다.
- `test_dupes.py`는 수정하지 않는다 — 최적화 후에도 계속 통과해야 한다.
