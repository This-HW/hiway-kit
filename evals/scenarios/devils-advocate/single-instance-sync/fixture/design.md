# 설계안: 실시간 재고 동기화 서비스

## 목표
매장 100개의 재고를 5초 이내에 중앙 DB와 동기화한다.

## 설계
- 각 매장 POS가 재고 변경 시마다 중앙 DB에 직접 HTTP POST로 즉시 반영한다.
- 중앙 DB는 단일 리전 단일 인스턴스(PostgreSQL)로 운영한다.
- 재시도/큐잉 없이 실패 시 클라이언트가 즉시 에러를 반환한다.
- 예상 트래픽: 매장당 초당 2건, 총 200 TPS.
- 인증은 매장별 고정 API 키를 코드에 하드코딩한다.
