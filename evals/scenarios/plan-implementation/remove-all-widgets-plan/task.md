# 과제

`src/inventory.py`에 `remove_all_widgets(name)` 함수를 추가하기 위한 구현
계획을 수립하라(실제 코드 작성 금지, 계획만).

계획에는 반드시 다음이 포함되어야 한다:
- 기존 `_stock` 데이터 구조 및 `add_widget`/`remove_widget`/`get_stock_level`과의
  관계
- 엣지 케이스(등록되지 않은 위젯, 이미 재고가 0인 위젯) 처리 방안
- 테스트 전략
