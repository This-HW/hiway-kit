class LRUCache:
    """TODO: implement a simple LRU (least-recently-used) cache. stdlib only.

    - __init__(self, capacity: int)
    - get(self, key) -> stored value, or -1 if key is missing. A successful
      get() must mark the key as most-recently-used.
    - put(self, key, value) -> stores value. If inserting a new key would
      exceed capacity, evict the least-recently-used entry first.
    """

    def __init__(self, capacity):
        raise NotImplementedError
