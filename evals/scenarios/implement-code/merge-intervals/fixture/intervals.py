def merge_intervals(intervals):
    """TODO: implement — merge overlapping (and touching) intervals.

    Input: list of [start, end] int pairs, in any order.
    Output: sorted list of merged [start, end] pairs with no overlaps.
    Must be a pure function (no side effects), stdlib only.
    """
    raise NotImplementedError
