def shout(text):
    """Return text uppercased with an exclamation mark appended."""
    return text.upper() + "!"


def slugify(text):
    """TODO: implement — convert text to a url-safe slug.

    Rules: lowercase, strip leading/trailing whitespace, collapse any run of
    whitespace/punctuation into a single '-', no leading/trailing '-'.
    e.g. "Hello World!" -> "hello-world"
    """
    raise NotImplementedError
